# Build output (dotnet)
**/bin/
**/obj/
artifacts/
*.binlog
*.log

# Secrets - NEVER commit
.env
.env.*
!.env.example
.env.local
.env.development
.env.production
config/api-keys.json
.api_key_encryption.key
*.pem
*.key

# Visual Studio / tooling caches
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates

# Python caches / virtual environments
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.venv/
.venvs/
venv/
venv_*/
env/

# Node / web tooling caches
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# XAML/compiler diagnostics and extracted dumps
.buildlogs/
xaml_bisect_tmp/
xc_stdout.txt
xc_stderr.txt
xaml_debug_chunk.txt
xaml_debug_include_*.txt

# Diagnostic artifacts (XAML compiler investigation)
t1_*.txt
t2_*.txt
xc_*.txt
input_noxbf*.json
diagnose_compiler.ps1
run_dumpbin.bat
run_xaml_compiler.bat
run_xaml_tests.bat
test_compiler_approot.ps1
test_xaml_compiler.ps1
xaml_debug.log

# Audit logs (local only, not committed)
.audit/

# Bewaker/Guardlock (tool artifacts, root policy 1.3)
.bewaker-audit.jsonl
.guardlock

# Local diagnostic captures (repo root)
_build_output.txt
_corecompile_errors.txt
build_errors.txt
build-output.log
build.log
msbuild.log
msbuild.binlog
build_diag.binlog

# Temporary working directory
temp/

# Local model binaries (do not commit)
models/**/*.pth
models/**/*.pt
models/**/*.onnx
models/**/*.bin
models/**/*.safetensors
models/**/*.ckpt

# Cursor workspace metadata (IDE/assistant)
.cursor/
openmemory.md

# SpecStory chat history (local IDE artifact)
.specstory/

# Audio uploads (user data)
data/audio_uploads/

# Backups and recordings (Cursor brick prevention)
backups/
data/recordings/

# PDF documents (generated, not source)
docs/**/*.pdf

# Output dumps and collection files
collect*.txt
full_collect.txt
errors_check.txt
unit_errors.txt
build_cs*.txt

# Root-level build artifacts and diagnostics (arch review 4.3, 4.8)
/api_test_output.txt
/build_warnings.txt
/build_errors.txt
/contract_test_output.txt
/test_health_output.txt
/rcs1163_warnings.txt
/rcs1163_files.txt
/warnings_full.txt
/temp_openapi.json
/xaml_stderr.txt
/xaml_stdout.txt
/final_test.wav
/test_output.wav
proof_runs/
_xaml_test/
.local/

# Installer build output (distribute separately)
installer/Output/

# Runtime bundle (downloaded/installed at build time)
installer/runtime/

# External runtime tools (local clones, not tracked in this repo)
runtime/external/

# Installer runtime binaries (local only, too large for git)
installer/runtime/

# Models directory (downloaded at runtime)
models/

# OpenMemory - IDE/Assistant specific rules
.cursor/rules/openmemory.mdc

backend/.venv/

# Runtime/tool artifacts (not source)
.agents/
.bewaker/
.voicestudio/
voicestudio.egg-info/
tools/chatgpt-localfiles/

# Local database (user data)
data/voicestudio.db
data/voicestudio_state.db
data/voicestudio_state.db-shm
data/voicestudio_state.db-wal
.cursor\rules\openmemory.mdc
AGENTS.md

input_noxbf_marker.txt

# Auto-generated build artifact (recreated by build tasks)
tools/print_xaml.targets

# Personal scratch notes (per-machine)
.notes/

# Per-machine session state (canonical copy is .cursor/STATE.md, already gitignored via .cursor/)
STATE.md
