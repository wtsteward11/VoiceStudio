VoiceStudioArchitecture Consolidation Plan — v1.2 WorkblockRepo-Grounded. ChatGPT Audit Corrected. 9 Source Files Inspected.Authored: 2026-03-11 | Baseline: v1.1.0 (2026-03-06) | Current Task: POST-CENTRALIZED-REQUEST-COORDINATIONFiles Inspected: STATE.md · DEFERRED_V1_2.md · ROADMAP_V2 · BackendClient.cs (4587 lines, 220 methods) ·BackendClient.Profiles.cs · BackendClient.Engines.cs · ProfilesViewModel.cs · IProfilePreviewService.cs ·ProfilePreviewService.cs · GracefulDegradationService.cs · RequestCoordinatorTests.cs · RequestCoordinatorIntegrationTests.cs · RateLimitToastDedupeTests.csCritical Correction: What ChatGPT Got WrongThe ChatGPT audit reviewed a snapshot of the repo that was multiple commits behind. Four of its eight tasks are already complete. Executing those tasks again would introduce regressions. This section documents the real state before the plan begins.ChatGPT TaskClaimed StatusActual Repo StateEvidenceTask 1: Route delete confirmation through IDialogServiceNEEDEDDONEProfilesViewModel.cs line 37: private readonly IDialogService? _dialogService. Lines 1241–1246 and 1547–1552 call _dialogService.ShowConfirmationAsync(...) with 'Delete profile?' and 'Delete profiles?' titles and 'Delete'/'Cancel' buttons.Task 2: Extract preview/playback to ProfilePreviewServiceNEEDEDDONEIProfilePreviewService.cs and ProfilePreviewService.cs both exist. ProfilePreviewService owns all three caches (_previewCache, _previewQualityCache, _previewQualityScoreCache), the synthesis call, the temp-file write, and the playback completion TCS. ProfilesViewModel.cs line 256 assigns _previewService from injected parameter.Task 3: Split BackendClient — profiles/engines partial classNEEDEDDONEBackendClient.Profiles.cs (173 lines) contains GetProfilesAsync, GetProfilesCoreAsync, CreateProfileAsync, UpdateProfileAsync, DeleteProfileAsync, InvalidateProfilesCache. BackendClient.Engines.cs contains GetEnginesAsync, GetEnginesCoreAsync, GetEngineRecommendationAsync.Task 4: Add RequestCoordinator unit + integration testsNEEDEDDONERequestCoordinatorTests.cs: 8 tests (concurrent coalesce, TTL, invalidation, prefix invalidation, cancellation, different types, TCS-gated factory). RequestCoordinatorIntegrationTests.cs: 3 tests with MockBackendHandler (profiles coalesce, engines coalesce, multi-panel startup). RateLimitToastDedupeTests.cs: 4 tests.MANDATE: Do not re-execute completed tasks. Doing so wastes sprint capacity and risks introducing regressions into working code. All four items above have live tests that would be invalidated or duplicated.Verified Current State — What Is Actually TrueThis is the state of the codebase as of 2026-03-11 based on direct file inspection. Not what the changelog says. Not what STATE.md summarises. What the source code contains.What Is Structurally CompleteAreaStatusEvidencev1.1.0 roadmap execution (Phases 0, A, B, C, D, E, F)COMPLETESTATE.md: Phase F COMPLETE. v1.1.0 shipped 2026-03-06.voice.py god-route eliminationCOMPLETEPhase A closed. voice/ submodule active. I-1 gate passing.Mediator dead code deletionCOMPLETEADR-046: backend/application/ deleted. Routes call services directly.RequestCoordinator (single-flight + TTL + invalidation)COMPLETERequestCoordinator.cs: 97 lines, lock-protected, in-flight + cache dict, finally-block cleanup.Profiles/engines request coordination wiredCOMPLETEBackendClient.Profiles.cs + BackendClient.Engines.cs both use _requestCoordinator.GetOrCreateAsync().ProfilePreviewService extractionCOMPLETEIProfilePreviewService.cs + ProfilePreviewService.cs. ViewModel uses _previewService field.Delete confirmation through IDialogServiceCOMPLETEProfilesViewModel.cs lines 1241–1246 and 1547–1552 call _dialogService.ShowConfirmationAsync.BackendClient profiles/engines partial class splitCOMPLETEBackendClient.Profiles.cs (173 lines) + BackendClient.Engines.cs (53 lines) exist.Request coordination testsCOMPLETE8 unit tests + 3 integration tests + 4 toast-dedupe tests all exist with passing assertions.Governance cleanup (CI suppression, Bandit B614, OpenAPI CI alignment, sentinel startup)COMPLETEDEFERRED_V1_2.md: all four items struck through as DONE 2026-03-11.What Is Structurally Weak — The Genuine Remaining Gap ListGapSeverityEvidenceIDialogService in ProfilesViewModel is nullable/optional — passing null silently suppresses all delete confirmationsHIGHConstructor line 240: IDialogService? dialogService = null. If PanelRegistry factory omits the argument, _dialogService is null. Lines 1241 and 1547 both use ?. operator: _dialogService?.ShowConfirmationAsync. If null, confirmed = false, delete silently aborts with no user feedback.BackendClient.cs is 4,587 lines with 220 public async methods spanning 16+ domains not yet partial-splitHIGHOnly Profiles (4 methods) and Engines (2 methods) are in partial files. The remaining 214+ methods — Audio, Mixer, Training, Quality, Workflows, Models, Plugins, Scripts, Backup, Settings, Transcription, Video, Batch, Effects, Pipeline, Macros — live in the main 4,587-line file. One regression there touches every panel.ProfilePreviewService caches are not thread-safeMEDIUMProfilePreviewService.cs uses three plain Dictionary<string,*> fields with no lock. If GetOrCreatePreviewAsync is called concurrently for different profiles, dict mutations race. WinUI 3 dispatches are synchronised, but test harnesses calling from thread pool will expose the race.GracefulDegradationService has no XAML-bindable property — the degraded-mode banner has no guaranteed UI surfaceMEDIUMGracefulDegradationService.cs: DegradedModeChanged is an EventHandler<bool>, not INotifyPropertyChanged. There is no ViewModel or shell property bound to a persistent InfoBar. DegradedModeClearHandler clears mode on 2xx, but there is no confirmed XAML element that shows when IsDegradedMode == true.UI-level endpoint-count proof does not exist for full panel workflowsMEDIUMRequestCoordinatorIntegrationTests prove coalescing for concurrent direct API calls. There is no proof harness that captures endpoint counts across realistic panel workflow: open panel → idle → create → reopen within TTL → explicit quality load. The gap matters because the ViewModel-layer wiring could still fan out.v1.2 deferred scope items have no execution breakdownLOW-NOW, HIGH-LATERDEFERRED_V1_2.md has three open items: skip debt cleanup (312+ skipped tests), workflow consolidation, strict mypy burn-down. Each is described in 1-2 sentences. Without scope, sequence, and owner, they will remain as-is into v1.3.No panel hardening pattern document existsLOWThe Profiles panel is now the de-facto reference implementation of correct panel architecture. There is no ADR or developer guide that captures the patterns (request coordination, dialog injection, selection-cancel, preview service boundary) for future panels to follow.Task 1 — Make IDialogService Non-Nullable in ProfilesViewModelSEVERITY: HIGH Silent delete suppression. If the factory omits the argument, all delete confirmations return false with no error, no log, no user feedback. This is a correctness bug masquerading as an architecture issue.1.1 Root CauseThe constructor signature on line 240 of ProfilesViewModel.cs reads:IDialogService? dialogService = null,This makes the dependency optional. Both delete call sites use the null-conditional operator:var confirmed = await _dialogService?.ShowConfirmationAsync(...) ?? false;If PanelRegistry does not pass the argument — which is possible since it is optional — the delete flow silently returns false at the confirmation step. The user sees nothing. The profile is not deleted. No error is logged.IDialogService is already implemented correctly in DialogService.cs. The Window-scoped singleton is already available via AppServices. The injection gap is purely in the constructor signature.1.2 What to ChangeStep 1: Remove the nullable/optional from the constructor parameterIn ProfilesViewModel.cs, change:IDialogService? dialogService = null,to:IDialogService dialogService,Step 2: Remove the null-conditional at each call siteFind both usages of _dialogService?. and replace:var confirmed = await _dialogService?.ShowConfirmationAsync(with:var confirmed = await _dialogService.ShowConfirmationAsync(This change is two characters per call site. It turns a silent suppression bug into a NullReferenceException at construction time if the factory omits the argument — which is the correct failure mode.Step 3: Update PanelRegistry factory to pass the argumentSearch PanelRegistry.cs for the factory lambda that constructs ProfilesViewModel. Confirm IDialogService is in the argument list. If it was being omitted (relying on the default null), add it. The singleton is available from AppServices.GetService<IDialogService>() or equivalent.Step 4: Add a null guard to the field assignmentIn the constructor body, replace:_dialogService = dialogService;with:_dialogService = dialogService ?? throw new ArgumentNullException(nameof(dialogService));This makes misconfiguration loud at startup, not silent at runtime.1.3 What NOT to ChangeDo not remove ConfirmationDialog.cs. Other view-layer call sites (ProfilesView.xaml.cs code-behind) may use it legitimately where XamlRoot is directly available.Do not add a ShowDeleteConfirmationAsync method to IDialogService. The existing ShowConfirmationAsync with appropriate title/message is sufficient. Avoid interface specialization creep.Do not make _dialogService a field with a fallback to ConfirmationDialog. That recreates the soft path through the back door.1.4 Verificationdotnet build VoiceStudio.sln -c Debug -p:Platform=x64 2>&1; echo BUILD=$LASTEXITCODEdotnet test src/VoiceStudio.App.Tests/ -c Debug -p:Platform=x64 -q 2>&1; echo TEST=$LASTEXITCODE.\scripts\verify.ps1 -Quick 2>&1; echo VERIFY=$LASTEXITCODE1.5 Definition of DoneProfilesViewModel constructor parameter is IDialogService (non-nullable, no default).No ?. operator used against _dialogService anywhere in the file.PanelRegistry factory passes a non-null IDialogService.Build exits 0. All tests pass. verify.ps1 -Quick exits 0.Task 2 — Fix ProfilePreviewService Cache Thread-SafetySEVERITY: MEDIUM Three plain Dictionary<string,*> fields with no lock. Safe under WinUI 3 UI-thread dispatch in production. Race condition exposed by any test harness calling from a thread pool. Fix before adding tests.2.1 Root CauseProfilePreviewService.cs contains:private readonly Dictionary<string, string> _previewCache = new();private readonly Dictionary<string, QualityMetrics?> _previewQualityCache = new();private readonly Dictionary<string, double> _previewQualityScoreCache = new();GetOrCreatePreviewAsync reads and writes all three dictionaries without any lock. In production, all calls route through the UI dispatch queue (single-threaded). In test harnesses that call the service from Task.WhenAll on the thread pool, multiple goroutines can race on Dictionary internals.The existing ProfilePreviewService has a second issue: no cleanup on cancellation. If GetOrCreatePreviewAsync is cancelled after the temp file is written but before the PlayFileAsync callback fires, the temp file leaks. The TaskCompletionSource is abandoned and the WaitAsync call throws OperationCanceledException — the finally block that deletes the temp file never runs.2.2 What to ChangeFix 1: Add a single lock object protecting all three cache dictionariesprivate readonly object _cacheLock = new();Wrap every read and write to the three dictionaries inside lock (_cacheLock) { ... }. The critical sections are short (dictionary lookups and assignments), so lock contention is negligible.Fix 2: Guarantee temp file cleanup on cancellationRefactor the temp-file + TCS pattern to use a try/finally that deletes the file:string? tempPath = null;try{ var audioBytes = await _httpClient.GetByteArrayAsync(audioUrl, ct); tempPath = Path.Combine(Path.GetTempPath(), $"vs_preview_{Guid.NewGuid():N}.wav"); await File.WriteAllBytesAsync(tempPath, audioBytes, ct); await _audioPlayer.PlayFileAsync(tempPath); return new PreviewResult(audioUrl, qualityMetrics, qualityScore);}finally{ if (tempPath != null && File.Exists(tempPath)) try { File.Delete(tempPath); } catch (IOException) { }}This removes the TaskCompletionSource entirely and makes cancellation safe.2.3 Tests to AddCreate: src/VoiceStudio.App.Tests/Services/ProfilePreviewServiceTests.csTest: ConcurrentPreviewRequests_DifferentProfiles_NoRaceCondition — Call GetOrCreatePreviewAsync for 5 different profile IDs from Task.WhenAll. Assert all succeed without dictionary exception.Test: GetOrCreatePreviewAsync_CachedProfile_DoesNotCallBackend — Preview profile A once. Preview again. Assert backend SynthesizeVoiceAsync called exactly once.Test: GetOrCreatePreviewAsync_BackendFailure_ReturnsNull — Mock backend throws. Assert method returns null, does not throw, does not write temp file.Test: StopPreview_CallsAudioPlayerStop — Call StopPreview. Assert IAudioPlayerService.Stop() was called.Test: TryGetCachedQuality_UnknownProfile_ReturnsFalse — Assert false is returned for a profile never previewed.2.4 Verificationdotnet test src/VoiceStudio.App.Tests/ --filter "ProfilePreviewService" -c Debug -p:Platform=x64 -q.\scripts\verify.ps1 -Quick 2>&1; echo VERIFY=$LASTEXITCODE2.5 Definition of DoneAll three cache dictionaries are protected by lock (_cacheLock).Temp file is deleted in a finally block, not in a TaskCompletionSource callback.5 new ProfilePreviewServiceTests pass.verify.ps1 -Quick exits 0.Task 3 — Continue BackendClient Domain Split (16 Remaining Domains)SEVERITY: HIGH (ongoing) BackendClient.cs: 4,587 lines, 220 public async methods. Only 6 methods are in partial files. The remaining 214+ methods have full file-level blast radius — one refactor or bug touches every panel.3.1 What Exists TodayPartial FileMethodsLinesStatusBackendClient.Profiles.csGetProfilesAsync, CreateProfileAsync, UpdateProfileAsync, DeleteProfileAsync, GetProfileAsync, InvalidateProfilesCache173DONEBackendClient.Engines.csGetEnginesAsync, GetEnginesCoreAsync, GetEngineRecommendationAsync53DONEBackendClient.cs (main)~214 methods across 16+ domains4,587 (minus the 226 in partials)NEEDS SPLITTING3.2 Recommended Extraction SequencePriority is determined by: (a) how many panels depend on the domain, (b) mutation rate, (c) how isolated the domain's methods already are.SprintDomainFile to CreateAnchor MethodsWhy First1QualityBackendClient.Quality.csAnalyzeQualityAsync, GetQualityHistoryAsync, GetQualityDegradationAsync, GetQualityBaselineAsync, GetQualityTrendsAsync (and 20+ variants)Highest method count in single domain. Quality is called from Profiles, Training, and Dashboard panels. Currently co-located with entirely unrelated Audio and Mixer code.1Audio / WaveformBackendClient.Audio.csGetAudioStreamAsync, UploadAudioFileAsync, GetWaveformDataAsync, GetSpectrogramDataAsync, GetAudioMetersAsync, GetRadarDataAsyncAudio analysis methods are pure data queries with no mutation side-effects. Easy to isolate.2TrainingBackendClient.Training.csStartTrainingAsync, GetTrainingStatusAsync, CancelTrainingAsync, GetTrainingLogsAsync, CreateDatasetAsync, ListDatasetsAsyncTraining is its own product surface. It should never be adjacent to Mixer or Backup code.2MixerBackendClient.Mixer.csGetMixerStateAsync, UpdateMixerStateAsync, CreateMixerSendAsync, UpdateMixerSendAsync, CreateMixerPresetAsync (and 20+ sub-methods)Mixer is a single large coherent domain with clean CRUD + preset pattern. Easy boundary.3ScriptsBackendClient.Scripts.csGetScriptsAsync, CreateScriptAsync, SynthesizeScriptAsync, AddSegmentToScriptAsyncSmall domain, clean surface, no shared state with other domains.3Batch JobsBackendClient.Batch.csCreateBatchJobAsync, GetBatchJobAsync, StartBatchJobAsync, CancelBatchJobAsync, GetBatchQueueStatusAsync, GetBatchQualityReportAsyncSelf-contained domain. Batch and Quality are currently interleaved — split both in the same sprint.4ProjectsBackendClient.Projects.csGetProjectsAsync, CreateProjectAsync, UpdateProjectAsync, DeleteProjectAsync, GetProjectAudioAsync, SaveAudioToProjectAsyncCore domain but broad. Leave for sprint 4 after the high-frequency domains are extracted.4Settings + BackupBackendClient.Settings.csGetSettingsAsync, SaveSettingsAsync, GetSettingsCategoryAsync; GetBackupsAsync, CreateBackupAsync, RestoreBackupAsyncTwo tiny domains that do not justify separate files. Co-locate in one partial.5Plugins + PipelineBackendClient.Plugins.csGetPluginHealthDashboardAsync, GetPluginMetricsAsync, ProcessPipelineAsync, GetPipelineProvidersAsyncLower-traffic domains. Extract last.3.3 How to Execute Each Split — The Repeatable PatternEvery domain split follows the same four-step sequence. Zero blast radius if executed correctly.Create the new partial file: BackendClient.[Domain].cs in the same directory as BackendClient.cs.Open with: namespace VoiceStudio.App.Services { public partial class BackendClient { ... } }Move the target methods (cut from BackendClient.cs, paste into partial). Include only: the methods themselves, domain-specific private helpers, domain-specific constants (endpoint strings, TTLs). Do NOT move: _httpClient, _requestCoordinator, _jsonOptions, ExecuteWithRetryAsync, CreateExceptionFromResponseAsync — these shared members stay in BackendClient.cs.Build immediately: dotnet build VoiceStudio.sln -c Debug -p:Platform=x64. Partial class compilation will catch any missing shared member references.RULE: One domain per commit. Never combine two domain splits in one commit. If the build breaks on a domain split, the diff must be small enough to revert cleanly.3.4 Definition of Done per SprintEach new partial file compiles cleanly in isolation.BackendClient.cs line count decreases by the moved method count after each sprint.No IBackendClient interface changes (all method signatures identical, just relocated).No caller changes required (partial class = same public surface).verify.ps1 -Quick exits 0 after each sprint's last commit.Task 4 — Wire Degraded Mode to a Persistent XAML SurfaceSEVERITY: MEDIUM GracefulDegradationService.DegradedModeChanged is an event. No confirmed XAML InfoBar binds to it. 429 flood suppression works (RateLimitToastDedupe), but there is no guaranteed non-modal surface that shows the user 'service is throttled.'4.1 What Exists vs. What Is MissingComponentStatusGapGracefulDegradationService.EnterDegradedMode / ExitDegradedModeEXISTSNot INotifyPropertyChanged. No XAML bindable property on the service itself.GracefulDegradationService.DegradedModeChanged event (EventHandler<bool>)EXISTSFired correctly. Nothing confirmed to subscribe to it in MainWindow or a shell ViewModel.DegradedModeClearHandler (HttpMessageHandler)EXISTSCalls ExitDegradedMode() on any 2xx. Correctly placed in handler chain.RateLimitToastDedupe (toast suppression for 429)EXISTS + TESTEDDedupes toasts. Does not trigger degraded mode. These are two separate paths.InfoBar or persistent degraded-mode banner in MainWindow.xamlUNCONFIRMEDNot found in files inspected. May exist but is not confirmed to bind to DegradedModeChanged.429 response handler calling EnterDegradedModeUNCONFIRMEDDegradedModeClearHandler clears on 2xx. It is not confirmed that 429 responses call EnterDegradedMode.4.2 What to Inspect FirstBefore writing code, read these files to confirm the actual gap:MainWindow.xaml — search for InfoBar, DegradedMode, x:Bind ViewModel.IsBackendDegradedMainWindow.xaml.cs — search for DegradedModeChanged subscription, banner open/closeBackendClient.cs (main) — search for 429, HttpStatusCode.TooManyRequests, EnterDegradedMode4.3 What to BuildStep 1: Wire 429 into degraded mode entryIn BackendClient.cs, in the response handling path (CreateExceptionFromResponseAsync or the 429-check in the handler chain), add:if ((int)response.StatusCode == 429){ _gracefulDegradation?.EnterDegradedMode( "Request rate limit reached", "synthesis", "quality-analysis"); // Do NOT show toast here — RateLimitToastDedupe handles the toast}Step 2: Add a bindable property to ShellViewModel or MainWindowViewModelGracefulDegradationService itself cannot implement INotifyPropertyChanged without making it a WinUI-layer dependency. Instead, create a thin shell ViewModel property:// In ShellViewModel.cs or MainWindowViewModel.cs:private bool _isBackendDegraded;public bool IsBackendDegraded{ get => _isBackendDegraded; private set { _isBackendDegraded = value; OnPropertyChanged(); }}Subscribe in the constructor:_degradationService.DegradedModeChanged += (_, isDegraded) =>{ Dispatcher.TryEnqueue(() => IsBackendDegraded = isDegraded);};Step 3: Add an InfoBar to MainWindow.xaml<InfoBar x:Name="DegradedModeBanner" Title="Service Throttled" Message="Requests are rate-limited. Some features are temporarily limited." Severity="Warning" IsOpen="{x:Bind ViewModel.IsBackendDegraded, Mode=OneWay}" IsClosable="False" />Step 4: Add testsTest: EnterDegradedMode_FiresDegradedModeChanged_True — Assert event fires with true.Test: ExitDegradedMode_After2xx_FiresDegradedModeChanged_False — Mock a 2xx response through DegradedModeClearHandler. Assert ExitDegradedMode was called.Test: BackendClient_429Response_CallsEnterDegradedMode — Mock a 429 response. Assert _gracefulDegradation.IsDegradedMode == true.4.4 User Experience ContractScenarioRequired BehaviorFirst 429 receivedEnterDegradedMode fires. IsBackendDegraded = true. InfoBar opens. Zero toasts (RateLimitToastDedupe suppresses).Second, third, Nth 429 receivedNo new toasts. InfoBar stays open. No state flicker.Any 2xx received after 429 periodDegradedModeClearHandler calls ExitDegradedMode. IsBackendDegraded = false. InfoBar closes.Backend offline (not 429, connection refused)Handled by BackendConnectionMonitor (separate). Offline toast shown once. InfoBar NOT shown (different state).App restartIsDegradedMode starts false (service is in-memory only, not persisted).4.5 Verificationdotnet build VoiceStudio.sln -c Debug -p:Platform=x64 2>&1; echo BUILD=$LASTEXITCODEdotnet test src/VoiceStudio.App.Tests/ --filter "Degraded|RateLimit|429" -c Debug -p:Platform=x64 -q.\scripts\verify.ps1 -Quick 2>&1; echo VERIFY=$LASTEXITCODETask 5 — Add UI-Level Panel Workflow Endpoint-Count ProofSEVERITY: MEDIUM Unit tests prove RequestCoordinator works in isolation. Integration tests prove BackendClient coalesces concurrent calls. Neither proves that the full ViewModel → BackendClient → RequestCoordinator chain holds under realistic panel workflows.5.1 What Already ExistsRequestCoordinatorIntegrationTests.cs covers three scenarios with a MockBackendHandler: concurrent GetProfilesAsync coalesces, concurrent GetEnginesAsync coalesces, multi-panel startup keeps both counts at 1.What these tests do NOT cover:ProfilesViewModel calling LoadProfilesAsync after CreateProfileAsync (invalidation + reload path).Panel reactivation within TTL (should hit cache, not backend).Explicit quality-load actions remaining separate from automatic actions.Selection change cancelling in-flight quality loads without triggering new list requests.5.2 What to BuildCreate: src/VoiceStudio.App.Tests/Integration/ProfilesPanelWorkflowTests.csTest Set A: Lifecycle coalescingTest: PanelLoad_IdleFor2s_ProfilesEndpointHitOnce — Simulate panel activation (LoadProfilesAsync). Wait 2 seconds. Assert GetSnapshot()["/api/profiles"] == 1.Test: PanelReactivate_WithinTTL_ProfilesEndpointNotHitAgain — Load profiles (1 hit). Simulate close + reopen (call LoadProfilesAsync again within 30s TTL). Assert still == 1 (cache served).Test: PanelReactivate_AfterTTLExpired_ProfilesEndpointHitAgain — Same as above but TTL expired via Invalidate(). Assert count == 2.Test Set B: Mutation invalidationTest: CreateProfile_ThenLoadProfiles_HitsBackendTwice — LoadProfilesAsync (1 hit). CreateProfileAsync (invalidates cache). LoadProfilesAsync again (1 more hit). Assert snapshot == 2.Test: UpdateProfile_ThenLoadProfiles_HitsBackendTwice — Same pattern with UpdateProfileAsync.Test: DeleteProfile_ThenLoadProfiles_HitsBackendTwice — Same pattern with DeleteProfileAsync.Test Set C: Selection-change safetyTest: SelectionChange_DoesNotTriggerListReload — Simulate SelectedProfile changing. Assert /api/profiles hit count does not increase (selection change must not auto-reload the list).Test: QualityLoadCommand_IsExplicitOnly_NotTriggeredByLoad — Call LoadProfilesAsync. Assert /api/quality/* endpoint count == 0 (quality is only loaded on explicit user command).5.3 Pass CriteriaScenarioEndpointMax CountPanel activation (single)/api/profiles1Idle after activation (2s)/api/profiles1 (unchanged)Reactivation within TTL/api/profiles1 (cache served)Create + reload/api/profiles (GET)2 (1 initial + 1 post-create)Selection change (any profile)/api/profiles0 additionalPanel load (automatic)/api/quality/*0 (quality is user-explicit only)5.4 Verificationdotnet test src/VoiceStudio.App.Tests/ --filter "ProfilesPanelWorkflow" -c Debug -p:Platform=x64 -q.\scripts\verify.ps1 -Quick 2>&1; echo VERIFY=$LASTEXITCODETask 6 — Break Down v1.2 Deferred Scope Into Executable SubplansSEVERITY: LOW-NOW, HIGH-LATER DEFERRED_V1_2.md has three open items described in 1-2 sentences each. Without scope, sequence, and criteria, these compound into v1.3 drag. Define them now while context is fresh.6.1 Skip Debt Cleanup — Subplan 6ACurrent state: 312+ tests are marked as skipped. Target: fewer than 200 by v1.2 ship.Step 1: Generate the skip inventorypython -m pytest tests/ --co -q 2>&1 | grep SKIP > docs/governance/skip_inventory_v1.2.txtStep 2: Categorise skips into three bucketsBucketDefinitionTarget ActionInfrastructure skipSkipped because real engine (XTTS, whisper) not available in CI. Expected in CI; not a code gap.Leave as-is. Mark with # INFRA-SKIP comment. Do not count against burn-down target.Product gap skipSkipped because the feature is not implemented yet (stub returns 404, ViewModel command is not wired).Create a GitHub issue or ADR task for each. These count against the burn-down target.Lazy skipSkipped with 'TODO: implement' and no clear owner. Feature may already work.Un-skip, run, fix failures, or delete if permanently irrelevant.Step 3: Set a per-batch burn-down scheduleBatch 1 (sprint 1): Categorise all 312+ skips. Remove or fix all lazy skips. Target: reduce to 260.Batch 2 (sprint 2): Fix all product-gap skips that have a backend route already implemented. Target: reduce to 220.Batch 3 (sprint 3): Fix or formally defer remaining product-gap skips. Target: reduce to <200 (v1.2 gate).Definition of Done for 6Askip_inventory_v1.2.txt exists in docs/governance/.All skips are categorised (INFRA-SKIP, product-gap, or lazy).Lazy skips are removed or converted to failing tests with an owner.Skip count < 200 before v1.2 ship.6.2 Workflow Consolidation — Subplan 6BCurrent state: Build, CI, Tests, and Sentinel workflows have overlapping jobs.Step 1: Map the current workflow graphCreate a table in docs/developer/WORKFLOW_MAP.md with: workflow file name, jobs, what each job does, dependencies, approximate runtime, and which jobs duplicate logic from another workflow.Step 2: Identify canonical vs. legacyOne workflow owns each category: build, test, lint, sentinel smoke.Other workflows that do the same thing are legacy convenience files — candidates for deletion or merge.CI caching (NuGet, pip, node_modules) must be defined in exactly one place and referenced.Step 3: Consolidate in two phasesPhase 1: Add comments to each workflow identifying its canonical vs. legacy status. No merges yet.Phase 2: Merge duplicate jobs. Delete legacy files. Confirm CI remains GREEN on all branches.Definition of Done for 6BWORKFLOW_MAP.md exists with full inventory.No two workflows run the same job for the same trigger without a documented reason.CI pipeline wall-clock time reduces by at least 15%.6.3 Strict Mypy Burn-Down — Subplan 6CCurrent state: Advisory only. mypy budget at 69/110 at time of ARCHITECT-HARDENING close.Step 1: Define enforcement scope by folderModuleTarget Enforcement LevelWhenbackend/core/--strictSprint 1 (least legacy code)backend/services/--strict (exclude legacy adapters)Sprint 2backend/api/routes/--disallow-untyped-defs --warn-return-anySprint 3tests/--ignore-missing-importsOngoing (not blocking)Step 2: Add per-folder mypy config in pyproject.toml[[tool.mypy.overrides]]module = ["backend.core.*"]strict = truedisallow_any_generics = trueStep 3: Set a hard CI gate per sprintSprint 1: backend/core/* must pass --strict. CI fails if any error in that folder.Sprint 2: backend/services/* added to strict gate.Sprint 3: backend/api/* added with relaxed flags.Definition of Done for 6Cpyproject.toml has per-folder [[tool.mypy.overrides]] sections.CI mypy job fails on first folder out of compliance.Total mypy error count < 30 by v1.2 ship.Task 7 — Codify and Publish the Panel Hardening PatternSEVERITY: LOW (but HIGH leverage) The Profiles panel is now the best-implemented panel in the repo. None of that architecture is documented. The next panel will repeat the same mistakes unless there is a canonical reference.7.1 What to DocumentCreate: docs/architecture/PANEL_HARDENING_PATTERN.md (or ADR-049 if it constitutes a decision)Section 1: Request Coordination ContractAll read endpoints that are shared across panels MUST use IRequestCoordinator.GetOrCreateAsync with an explicit TTL.All write endpoints MUST call Invalidate or InvalidateByPrefix after mutation.ViewModels must not implement local coalescing — push coordination to BackendClient partial.// CORRECT — in BackendClient.[Domain].cs:public async Task<List<T>> GetItemsAsync(CancellationToken ct = default) => await _requestCoordinator.GetOrCreateAsync("domain:list", GetItemsCoreAsync, TTL, ct);Section 2: Dialog Injection ContractViewModels MUST receive IDialogService as a required non-nullable constructor parameter.ViewModels must NEVER call ConfirmationDialog static helpers.Destructive confirmation must always use: _dialogService.ShowConfirmationAsync(title, message, "Delete", "Cancel").View code-behind MAY call ConfirmationDialog only when XamlRoot is directly available and the popup is not triggered by a ViewModel command.Section 3: Selection-Change Cancellation PatternMaintain a _selectionChangeCts: CancellationTokenSource?.On SelectedProfile change: cancel existing CTS, allocate new one, link to disposal token.All quality-related async calls MUST take the linked token.Never call quality analytics automatically on selection change — require explicit user command.Section 4: Preview and Playback ContractPreview synthesis, caching, temp-file lifecycle, and playback coordination MUST live in IProfilePreviewService (or domain equivalent).ViewModels own: command wiring, IsPreviewing state, error display. Not cache dictionaries.Cache dictionaries used by services MUST be protected by a lock object.Section 5: Proof RequirementEvery new panel must be accompanied by at least one endpoint-count proof test covering: panel activation, idle period, and one mutation + reload.Proof tests must fail if request coordination is removed.7.2 Definition of DonePANEL_HARDENING_PATTERN.md exists with all five sections.At least one other panel's code is checked against the pattern (can be an issue filed, not a code fix).CLAUDE.md references the new document in its Reference Index.Execution ScheduleThese tasks are sequenced by risk reduction per unit of effort. Do not reorder them.BlockTasksGoalSuccess Gate1T1 + T2Correctness hardening — no silent bugs, no race conditionsIDialogService non-nullable. Preview caches locked. 5 new preview tests pass. verify.ps1 exits 0.2T3 (Sprint 1: Quality + Audio)Start BackendClient blast-radius reduction — highest domain firstBackendClient.Quality.cs and BackendClient.Audio.cs exist. BackendClient.cs is measurably shorter. Build clean.3T4 + T5Close the proof and UX gaps — degrade gracefully, prove behavior429 → InfoBar wired. 8 panel workflow tests passing. Endpoint counts bounded.4T3 (Sprints 2–3: Training, Mixer, Scripts, Batch)Continue domain split — medium-traffic domains4 more partial files exist. BackendClient.cs down to ~3000 lines.5T6 (6A + 6B + 6C)v1.2 debt reduction — executable subplans runningSkip inventory done. WORKFLOW_MAP.md done. mypy Sprint 1 gate passing.6T7 + T3 (final sprints)Pattern documentation + remaining BackendClient domainsPANEL_HARDENING_PATTERN.md published. BackendClient.cs down to ~1800 lines.What Not to Do — Anti-Patterns for This Work BlockAnti-PatternWhy It Is DestructiveRe-execute Tasks 1–4 from the ChatGPT auditAll four are already done. Doing them again wastes a sprint, creates duplicate code, and breaks tests that already verify the correct implementation.Attempt to split IBackendClient into 16 interfaces in one passEvery ViewModel, every test, every DI registration must change simultaneously. This is a multi-quarter effort. Partial class split is the correct first move — same public surface, zero caller changes.Rewrite ProfilesViewModel from scratchIt is oversized but it works. Shrink it incrementally by pushing responsibilities to services. A full rewrite introduces new bugs in every tested path.Add IProfilePreviewService to other panels before the pattern is documentedWithout a canonical pattern document, each panel will evolve slightly differently. Document the pattern first (Task 7), then apply consistently.Chase strict mypy before BackendClient blast-radius is reducedmypy errors in core.py do not threaten production. A bug in 4,587-line BackendClient.cs does. Priority is blast-radius reduction, not annotation completeness.Declare 'architecture is done' after any single taskArchitecture is never done. It degrades toward entropy unless you actively compress it. The goal is not a declaration — it is a measurable line count and test count that reflects reduced risk.Definition of the Entire Work Block Being CompleteThe v1.2 work block is done when every item in this list is true simultaneously:ProfilesViewModel.cs constructor takes IDialogService as non-nullable. No ?. used against _dialogService. PanelRegistry factory passes a non-null instance.ProfilePreviewService caches are locked. Temp file is cleaned up in a finally block, not in a TCS callback. 5 ProfilePreviewServiceTests pass.BackendClient.Quality.cs and BackendClient.Audio.cs exist as partial class files. BackendClient.cs is measurably shorter. Build exits 0.429 responses enter degraded mode. IsBackendDegraded property is bindable. InfoBar is confirmed in MainWindow.xaml. DegradedModeClearHandler clears on 2xx. At least 3 degraded-mode tests pass.8 ProfilesPanelWorkflowTests exist and pass. All endpoint-count assertions hold.Skip inventory exists in docs/governance/. All lazy skips are removed or converted. Skip count trajectory is on track for <200 at v1.2 ship.PANEL_HARDENING_PATTERN.md exists with all five sections. CLAUDE.md references it.verify.ps1 -Quick exits 0 after each task's final commit.The measure of this work block is not features shipped. It is risk surface reduced and behavior proven.Four tasks ChatGPT thought were needed are already done. Build on what is real, not on what a stale review assumed.Plan authored: 2026-03-11 | Grounded in 14 source files inspected directly