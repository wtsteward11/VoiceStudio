# MainWindow Decomposition Plan

**Status:** Living  
**Last Updated:** 2026-04-27 (Slice 40 window-activated exception logging shell; Slice 39 smoke / safe-startup mode shell; Slice 38 keyboard shortcut key dispatch shell; Slice 37 panel quick-switch shortcut registration shell; Slice 36 keyboard shortcut registration shell; Slice 35 tool catalog panel host chrome shell; Slice 34 menu bar build shell; Slice 33 workspace grid splitter shell; Slice 32 shell chrome Mica/title bar/theme; Slice 31 import-audio / import workflow shell; Slice 30 global transport + timeline; Slice 27 panel region focus + cycling; Slice 26 startup backend overlay; Slice 25 panel docking / cross-region swap; Slice 24 panel quick-switch indicator; Slice 23 nav panel preview; Slice 22 recent projects menu population; Slice 21 keyboard shortcuts)  
**Related:** Transport Coherence Wave 2 Task 9, MainWindow god-object reduction

## Overview

MainWindow.xaml.cs is ~2450 lines across multiple partials. This plan identifies extraction targets to reduce the god-object status. Each slice is scoped and actionable.

## Current Structure

| Partial | Responsibility | Lines (approx) |
|---------|----------------|----------------|
| MainWindow.xaml.cs | Core shell, panel creation, Loaded, navigation, transport, status metrics bridge wiring | ~2450 |
| MainWindow.Workspaces.cs | Workspace layout, splitter, restore | — |
| `MainWindowMenuBarShellBridge` (Services) | Top-level `MenuBar` build; `MainWindow.Menu.cs` removed (Slice 34) | — |
| `MainWindowToolCatalogPanelHostChromeShellBridge` (Services) | Tool catalog → `PanelHost` title/icon after open (Slice 35) | — |
| `MainWindowSmokeStartupModeShellBridge` (Services) | Gate-C smoke / safe-startup env + command-line probes (Slice 39) | — |
| `MainWindowWindowActivatedLoggingShellBridge` (Services) | `MainWindow_Activated` try/catch + `ErrorLogger.LogWarning`; forwards to welcome bridge (Slice 40) | — |
| `MainWindowKeyboardShortcutKeyDispatchShellBridge` (Services) | Root `KeyDown` → `TryHandleKeyDown` with modifier bitmask (Slice 38) | — |
| `MainWindowPanelQuickSwitchShortcutRegistrationShellBridge` (Services) | Ctrl+1–9 `nav.panel.{n}` registrations after Slice 36 `Register` (Slice 37) | — |
| `MainWindowKeyboardShortcutRegistrationShellBridge` (Services) | Bulk `KeyboardShortcutService` ctor registrations (Slice 36); panel quick-switch table → **Slice 37** | — |
| MainWindow.Smoke.cs | Smoke test orchestration | — |

## Already Extracted

- **Transport orchestration:** `GlobalTransportOrchestrator` + `TransportOrchestrationBootstrap` (Transport Coherence Wave 1)
- **Status Bar Orchestration:** `StatusBarCoordinator` (Transport Coherence Wave 3) — Attach/Subscribe/Unsubscribe; current media, reachability, degraded banner
- **Transport Shortcut Orchestration:** `TransportShortcutCoordinator` (Transport Coherence Wave 4 Phase 1) — Space/S/Ctrl+R via IGlobalTransportOrchestrator
- **Import Workflow:** `IImportWorkflowService` + `ImportWorkflowService` (Transport Coherence Wave 4 Phase 2) — picker, upload, AssetAddedEvent, SetCurrentPlayable, toast
- **GAP-008 Slice 4 — Project workflow entry points:** `MainWindowProjectWorkflowBridge` — File menu New/Open/Save + `OpenRecentProject` forward to `IProjectWorkflowCoordinator` only ([Slice 4 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE04_MAINWINDOW_PROJECT_WORKFLOW.md); Choice A).
- **GAP-008 Slice 5 — Recent-project mutations:** `MainWindowRecentProjectsMutationBridge` + `IRecentProjectsMutationCommands` — Pin/Unpin/Clear/Remove + toasts (except Remove: no toast); flyout **composition** moved to **`MainWindowRecentProjectsMenuPopulationShellBridge`** in **Slice 22** ([Slice 5 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE05_MAINWINDOW_RECENT_PROJECTS_MUTATION.md) — Option A superseded for **where** population lives; [Slice 22 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE22_MAINWINDOW_RECENT_PROJECTS_MENU_POPULATION_SHELL.md)).
- **GAP-008 Slice 6 — Global search overlay shell glue:** `MainWindowSearchOverlayShellBridge` — show / navigate / background-tap dismiss / startup overlay collapsed; forwards to **`ISearchOverlayCoordinator`** only ([Slice 6 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE06_MAINWINDOW_SEARCH_OVERLAY_SHELL.md); toolbar **not** in this slice).
- **GAP-008 Slice 7 — Toolbar customization shell glue:** `MainWindowToolbarCustomizationShellBridge` + `IToolbarCustomizationDialogLauncher` — Tools menu “Customize Toolbar…” → `ToolbarCustomizationDialog` only ([Slice 7 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE07_MAINWINDOW_TOOLBAR_SHELL.md)); command palette and tool catalog **not** in this slice.
- **GAP-008 Slice 8 — Command palette shell glue:** `MainWindowCommandPaletteShellBridge` + `ICommandPaletteShellLauncher` — Ctrl+P / `ShowCommandPalette` → `CommandPaletteService` only ([Slice 8 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE08_MAINWINDOW_COMMAND_PALETTE_SHELL.md)); tool catalog **not** in this slice.
- **GAP-008 Slice 9 — Toolbar command routing shell glue:** `MainWindowToolbarCommandShellBridge` + `IToolbarShellImportFromToolbar` — toolbar **`import_audio`** → `ImportAudioFile` without `App.MainWindowInstance` ([Slice 9 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE09_MAINWINDOW_TOOLBAR_COMMAND_ROUTING_SHELL.md)); tool catalog **not** in this slice.
- **GAP-008 Slice 10 — Tool catalog shell glue:** `MainWindowToolCatalogShellBridge` + `IToolCatalogShellLauncher` + `ToolCatalogShellLauncher` — **`ShowToolCatalogAsync`** / **`nav.toolcatalog`** → **`ToolCatalogDialog`**; post-confirm **`OpenPanelByIdAsync`** + **`PanelHost`** chrome **via** **`WireToolCatalogHandlers`** (chrome **implementation** moved to **`MainWindowToolCatalogPanelHostChromeShellBridge`** in **Slice 35**; **[Slice 10 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE10_MAINWINDOW_TOOL_CATALOG_SHELL.md)**); command palette / toolbar / search **not** in this slice.
- **GAP-008 Slice 35 — Tool catalog panel host chrome shell:** `MainWindowToolCatalogPanelHostChromeShellBridge` — **`Apply(PanelRegion, string title, string? icon, findNameOnContent)`** for **`LeftPanelHost` / `CenterPanelHost` / `RightPanelHost` / `BottomPanelHost`**; **`MainWindow` ctor** wires chrome bridge then **`WireToolCatalogHandlers`** second arg → **lambda** to **`Apply`** ([Slice 35 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE35_MAINWINDOW_TOOL_CATALOG_PANEL_HOST_CHROME_SHELL.md)); **not** **Slice 10** dialog (`RunShowAsync`), not bulk shortcut registration (**Slice 36**).
- **GAP-008 Slice 39 — Smoke / safe-startup mode shell:** `MainWindowSmokeStartupModeShellBridge` — **`IsSafeStartupMode` / `IsGateCSmokeMode`** off **`MainWindow`**; **`EvaluateSafeStartup`** for **`ShellNavigationCoordinator`** (dedupe); **not** **Slice 38** **`KeyDown`** dispatch, **not** **Slice 11** **`HandleActivatedAsync`** ([Slice 39 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE39_MAINWINDOW_SMOKE_STARTUP_MODE_SHELL.md)).
- **GAP-008 Slice 40 — Window-activated exception logging shell:** `MainWindowWindowActivatedLoggingShellBridge` — **`RunActivatedAsync`** **try/catch** + **`ErrorLogger.LogWarning`** for **`MainWindow_Activated`**; **`MainWindow`** thin forward to **`MainWindowStartupWelcomeActivationShellBridge.HandleActivatedAsync`**; **not** **Slice 11** welcome orchestration body, **not** **Slice 39** smoke predicates ([Slice 40 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE40_MAINWINDOW_WINDOW_ACTIVATED_LOGGING_SHELL.md)).
- **GAP-008 Slice 38 — Keyboard shortcut key dispatch shell:** `MainWindowKeyboardShortcutKeyDispatchShellBridge` — **`MainWindow_KeyDown`** → **`TryHandleKeyDown`** (modifiers + service); **not** **Slice 36/37** **`RegisterShortcut`** ([Slice 38 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE38_MAINWINDOW_KEYBOARD_KEY_DISPATCH_SHELL.md)); **not** **Slice 21** Help **`ContentDialog`** flow.
- **GAP-008 Slice 37 — Panel quick-switch shortcut registration shell:** `MainWindowPanelQuickSwitchShortcutRegistrationShellBridge` — **Ctrl+1–9** **`nav.panel.{n}`** after **`MainWindowKeyboardShortcutRegistrationShellBridge.Register`** ([Slice 37 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE37_MAINWINDOW_PANEL_QUICK_SWITCH_SHORTCUT_REGISTRATION_SHELL.md)); **not** **Slice 24** indicator **`Popup`**, **not** **Slice 36** file/edit/zoom bulk table.
- **GAP-008 Slice 36 — Keyboard shortcut registration shell:** `MainWindowKeyboardShortcutRegistrationShellBridge` + **`MainWindowKeyboardShortcutRegistrationDependencies`** — ctor **`RegisterShortcut`** bulk (**file / edit / nav / zoom / help / panel focus**); **Ctrl+1–9** panel table → **Slice 37** ([Slice 36 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE36_MAINWINDOW_KEYBOARD_SHORTCUT_REGISTRATION_SHELL.md)); **Slice 21** **`MainWindowKeyboardShortcutsShellBridge`** = Help **dialog** only.
- **GAP-008 Slice 17 — Status strip clock shell:** `MainWindowStatusStripClockShellBridge` — one-minute **`ClockText`** wall clock + dispose ([Slice 17 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE17_MAINWINDOW_STATUS_STRIP_CLOCK_SHELL.md)).
- **GAP-008 Slice 18 — Status strip metrics shell:** `MainWindowStatusStripMetricsShellBridge` — 2s **`DispatcherTimer`** CPU/RAM/GPU/latency + async health/telemetry ([Slice 18 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE18_MAINWINDOW_STATUS_STRIP_METRICS_SHELL.md)); **`MainWindow.StatusBar.cs`** removed.
- **GAP-008 Slice 19 — Status bar coordinator shell wiring:** `MainWindowStatusBarCoordinatorShellBridge` — **`StatusBarCoordinator`** resolve + **`Attach`** + **`Subscribe`** + Loaded-bootstrap prelude **`StartBackendHealthMonitoring`** only ([Slice 19 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE19_MAINWINDOW_STATUS_BAR_COORDINATOR_SHELL.md)); **`StatusBarCoordinator`** implementation unchanged; lifetime **`Unsubscribe`** stays on **`MainWindow`** via **`MainWindowLifetimeCleanupShellBridge`** channel.
- **GAP-008 Slice 20 — Menu / tool activation shell:** `MainWindowMenuToolActivationShellBridge` — check-for-updates, mini-timeline toggle, collaboration panel visibility, manage workspaces ([Slice 20 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE20_MAINWINDOW_MENU_TOOL_ACTIVATION_SHELL.md)); keyboard shortcuts menu flow **not** in this bridge (**Slice 21**).
- **GAP-008 Slice 21 — Keyboard shortcuts shell:** `MainWindowKeyboardShortcutsShellBridge` — **`KeyboardShortcutsMenuItem_Click`** → **`KeyboardShortcutsView`** / **`KeyboardCustomizationView`** **`ContentDialog`** flow only ([Slice 21 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE21_MAINWINDOW_KEYBOARD_SHORTCUTS_SHELL.md)); **`MainWindow`** one-line forward + ADR-047 timing unchanged.
- **GAP-008 Slice 22 — Recent projects menu population shell:** `MainWindowRecentProjectsMenuPopulationShellBridge` — **`PopulateRecentProjectsMenu`** flyout **`MenuFlyout*`** tree + click wiring to **`MainWindowProjectWorkflowBridge`** / **`MainWindowRecentProjectsMutationBridge`** via ctor delegates only ([Slice 22 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE22_MAINWINDOW_RECENT_PROJECTS_MENU_POPULATION_SHELL.md)); **`MainWindow`** thin forward only.
- **GAP-008 Slice 23 — Nav rail panel preview shell:** `MainWindowPanelPreviewShellBridge` — nav **`PointerEntered` / `Exited`** → **`PanelPreviewPopup`**; **`GetPanelInfoForButton`** / **`CreatePreviewContent`**; hide timer + **`DisposePreviewHideTimer`** for lifetime prelude ([Slice 23 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE23_MAINWINDOW_PANEL_PREVIEW_SHELL.md)); **`MainWindow`** two-line event forwards only.
- **GAP-008 Slice 24 — Panel quick-switch visual indicator shell:** `MainWindowPanelQuickSwitchShellBridge` — IDEA 1 **`Popup` + `PanelQuickSwitchIndicator` + hide timers**; **`ShowPanelQuickSwitchIndicator`** for **`ShellNavigationCoordinator`** + **`SwitchToPanel`**; **panel region focus** label path via **`MainWindowPanelRegionFocusShellBridge`** (Slice 27) **injected** callback — **`DisposeQuickSwitchHideTimer`** on **`MainWindowLifetimeCleanupCoreChannels`** ([Slice 24 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE24_MAINWINDOW_PANEL_QUICK_SWITCH_INDICATOR_SHELL.md)); **`MainWindow`** thin delegates only.
- **GAP-008 Slice 25 — Panel docking / cross-region swap shell:** `MainWindowPanelDockShellBridge` — IDEA 14 **`OnPanelDockRequested`**, cross-region **`PanelHost`**, fade **storyboard**, state migrate, **`OpenPanelByIdAsync`** + layout save + toasts only ([Slice 25 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE25_MAINWINDOW_PANEL_DOCKING_SHELL.md)); **`MainWindow`** region map + ctor + **`+=` event wiring** only.
- **GAP-008 Slice 27 — Panel region focus + cycling shell:** `MainWindowPanelRegionFocusShellBridge` — **Ctrl+Tab** / **Ctrl+Shift+Tab** / **Ctrl+Alt+1–4** **Focus** on **`PanelHost`**, cycling index, optional quick-switch **label** via **injected** **`Action`** to Slice 24 indicator ([Slice 27 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE27_MAINWINDOW_PANEL_REGION_FOCUS_SHELL.md)); shortcut **registration** for these routes lives in **`MainWindowKeyboardShortcutRegistrationShellBridge`** (**Slice 36**); **not** **IDEA 1** **Popup** implementation, not workspace splitter wholesale.
- **GAP-008 Slice 26 — Startup backend overlay shell:** `MainWindowStartupOverlayShellBridge` — **`IStartupStateService`** overlay visibility, message/progress, **`StartupRetryButton`**, cold-start **shell-interactive** timing via **`Action`** from **`MainWindow`** (owns **`_recordedShellInteractiveTiming`**) ([Slice 26 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE26_MAINWINDOW_STARTUP_OVERLAY_SHELL.md)); **`MainWindow`** thin **`StateChanged`** / retry / initial **`ApplyStartupOverlay`** only; **not** welcome dialog (**`MainWindowStartupWelcomeActivationShellBridge`**).
- **GAP-008 Slice 28 — Help / About shell:** `MainWindowHelpAboutShellBridge` — **Help** → **Documentation Folder** (Explorer) + **About** `ContentDialog` ([Slice 28 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE28_MAINWINDOW_HELP_ABOUT_SHELL.md)); **not** Edit Undo/Redo (**Slice 29**).
- **GAP-008 Slice 29 — Edit — Undo / Redo shell:** `MainWindowEditUndoRedoShellBridge` — **Edit** menu **Undo** / **Redo** + **`edit.undo`** / **`edit.redo`** keyboard routes → **`UndoRedoService`** + error log on failure ([Slice 29 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE29_MAINWINDOW_EDIT_UNDO_REDO_SHELL.md)); **not** **MainWindowKeyboardShortcutsShellBridge** dialog stack (**Slice 21**), not transport.
- **GAP-008 Slice 30 — Global transport + timeline shell:** `MainWindowGlobalTransportShellBridge` — **Play/Stop**, **ToggleRecording**, **OpenRecordingPanelFromTransportShortcut**, timeline **zoom** + **`RegisterKeyboardShortcuts`** zoom ([Slice 30 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE30_MAINWINDOW_GLOBAL_TRANSPORT_TIMELINE_SHELL.md)); **not** import-audio path (**Slice 31**).
- **GAP-008 Slice 31 — Import-audio shell:** `MainWindowImportWorkflowShellBridge` — **File** menu + **`file.import`** + **toolbar** **`ImportAudioFile`** forward: **`IStartupStateService`** gate, **`IImportWorkflowService`**, **HWND** to picker ([Slice 31 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE31_MAINWINDOW_IMPORT_WORKFLOW_SHELL.md)); **not** `MainWindowProjectWorkflowBridge` New/Open/Save (**Slice 4**), not **`IImportWorkflowService`** implementation.
- **GAP-008 Slice 32 — Shell chrome (Mica, title bar, theme):** `MainWindowShellChromeShellBridge` — **`ApplyMicaBackdrop`**, **`InitializeCustomTitleBar`**, **`UnsubscribeShellChromeEvents`**; `MaterialsHelper` + **`AppWindow.TitleBar`** + **`IUnifiedThemeService.ThemeChanged`**; **`MainWindow.Shell` partial** one-line forwards only ([Slice 32 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE32_MAINWINDOW_SHELL_CHROME_MICA_TITLEBAR.md)); **not** Loaded bootstrap structure (**Slice 1**), not **`MainWindowImportWorkflowShellBridge`** (**Slice 31**).
- **GAP-008 Slice 33 — Workspace grid splitter shell:** `MainWindowWorkspaceSplitterShellBridge` — **`WorkspaceGrid`** pointer splitters **`VerticalSplitter1`**, **`VerticalSplitter2`**, **`HorizontalSplitter`**; star column/row resize + debounced layout save on release; **`MainWindow.Workspaces` partial** one-line forwards only ([Slice 33 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE33_MAINWINDOW_WORKSPACE_GRID_SPLITTER_SHELL.md)); **not** full workspace init/restore (**`InitializePanelsAsync`**, etc.), not **Slice 32** chrome.
- **GAP-008 Slice 34 — Menu bar build shell:** `MainWindowMenuBarShellBridge` — top-level **`MenuBar`** (File/Edit/View/Modules/Playback/Tools/AI/Help) + **`IPanelRegistry.GetAllDescriptors()`** for Modules; pre-built **flyout** items from **`MainWindow` ctor** + **`MainWindowMenuBarCommandCallbacks`**; **former** `MainWindow.Menu.cs` **removed** ([Slice 34 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE34_MAINWINDOW_MENU_BAR_BUILD_SHELL.md)); **not** **Slice 22** **`PopulateRecentProjectsMenu`** (Loaded), not **Slice 20** / **21** / **32** / **33** seams.

### Narrow-seam guardrails (do not expand without new bounded slice + ADR)

**Rule:** None of the following becomes a junk drawer. New shell behavior → new bounded brief or explicit amendment.

- **`MainWindowShellLoadedBootstrap`** — Loaded bootstrap orchestration only ([Slice 1 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE01_MAINWINDOW_LOADED_BOOTSTRAP.md)); not general-purpose shell events. **Forbidden:** a second unrelated shell responsibility in the same type without a new bounded brief + tests.
- **`NavButtonActionSink`** — [Slice 2 brief §Guardrail](VOICESTUDIO_BOUNDED_GAP008_SLICE02_MAINWINDOW_NAVIGATION_SHELL.md): ctor-ordering bridge for nav highlight only; not an event bus. **Forbidden:** routing unrelated commands or panel opens through this sink.
- **`MainWindowNavigationShellBridge`** — Navigation-shell glue only ([Slice 2 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE02_MAINWINDOW_NAVIGATION_SHELL.md)); not toolbar, not search execution. **Forbidden:** palette, tool catalog, or overlay dismiss logic.
- **`MainWindowLoadedTailBootstrap`** / **`MainWindowLoadedTailHooks`** — Loaded-order **transport attach + panel-init trigger** only ([Slice 3 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE03_MAINWINDOW_LOADED_TAIL.md)). **Forbidden:** user-facing dialogs or palette/catalog launch from the tail runner.
- **`MainWindowProjectWorkflowBridge`** — **Coordinator entry-point façade** only (Choice A); not a menu builder, not import/jump/file/session logic. **Forbidden:** embedding workflow implementation or non-coordinator APIs.
- **`MainWindowRecentProjectsMutationBridge`** — **Recent list mutations + toasts** only ([Slice 5 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE05_MAINWINDOW_RECENT_PROJECTS_MUTATION.md)); not flyout population, not File New/Open/Save, not import. **Forbidden:** file-picker or project-open orchestration in the bridge.
- **`MainWindowSearchOverlayShellBridge`** — **`MainWindow` → `ISearchOverlayCoordinator`** wiring for global search overlay only ([Slice 6 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE06_MAINWINDOW_SEARCH_OVERLAY_SHELL.md)); not toolbar, not search query execution, not panel routing implementation. **Forbidden:** command palette, toolbar customization, or tool catalog.
- **`MainWindowToolbarCustomizationShellBridge`** — **Toolbar customization dialog** from shell only ([Slice 7 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE07_MAINWINDOW_TOOLBAR_SHELL.md)); not command palette, not tool catalog, not search overlay. **Forbidden:** `CommandPaletteService`, `ShowToolCatalogAsync`, or search-overlay coordinator calls.
- **`MainWindowCommandPaletteShellBridge`** — **Command palette open from shell** only ([Slice 8 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE08_MAINWINDOW_COMMAND_PALETTE_SHELL.md)); not toolbar customization, not search overlay, not tool catalog (`OpenPanelByIdAsync`). **Forbidden:** any second shell feature in this file without a new bounded brief + tests.
- **`MainWindowToolbarCommandShellBridge`** — **Toolbar import-audio routing from shell** only ([Slice 9 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE09_MAINWINDOW_TOOLBAR_COMMAND_ROUTING_SHELL.md)); not toolbar customization, not command palette, not search overlay, not tool catalog, not `IUnifiedCommandRegistry` internals. **Forbidden:** routing play/stop/undo or workspace/engine UI through this bridge without a new bounded brief + tests.
- **`MainWindowToolCatalogShellBridge`** — **Tool catalog dialog + confirm → open panel + chrome** from shell only ([Slice 10 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE10_MAINWINDOW_TOOL_CATALOG_SHELL.md)); not command palette, not search overlay, not toolbar customization, not toolbar command routing, not navigation-shell implementation. **Forbidden:** any second shell feature in this file without a new bounded brief + tests.
- **`MainWindowToolCatalogPanelHostChromeShellBridge`** — **`PanelHost` title/icon** for the **tool catalog** completion path only ([Slice 35 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE35_MAINWINDOW_TOOL_CATALOG_PANEL_HOST_CHROME_SHELL.md)); not **`RunShowAsync`**, not **keyboard shortcut registration** bulk (**Slice 36**), not **Ctrl+1–9** panel registration (**Slice 37**), not root **`KeyDown`** shortcut dispatch (**Slice 38**), not **command palette**. **Forbidden:** accreting non–tool-catalog panel chrome without a new bounded brief + tests.
- **`MainWindowPanelQuickSwitchShortcutRegistrationShellBridge`** — **Ctrl+1–9** **`nav.panel.{n}`** **`RegisterShortcut`** only ([Slice 37 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE37_MAINWINDOW_PANEL_QUICK_SWITCH_SHORTCUT_REGISTRATION_SHELL.md)); not **Slice 24** **`Popup`/indicator/timers**, not **Slice 27** region-focus cycling, not **Slice 36** bulk shortcut table. **Forbidden:** referencing **`MainWindowPanelQuickSwitchShellBridge`** in this file (creep guard) or merging indicator with registration.
- **`MainWindowSmokeStartupModeShellBridge`** — **`VOICESTUDIO_SAFE_STARTUP`** + Gate-C smoke env / command-line / argv classification only ([Slice 39 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE39_MAINWINDOW_SMOKE_STARTUP_MODE_SHELL.md)); **`MainWindow`** ctor field before panel-region / coordinator / welcome **`Func<bool>`** wiring; **`ShellNavigationCoordinator`** uses **`EvaluateSafeStartup()`** only for the duplicate safe-mode path. **Forbidden:** **`MainWindow_Activated`** / **`WelcomeView`** orchestration (**Slice 11**), **Slice 38** **`KeyDown`** dispatch, shortcut **registration** (**Slices 36–37**). **Forbidden:** referencing **`MainWindowKeyboardShortcutKeyDispatchShellBridge`** or **`RegisterShortcut`** in this file (creep guard).
- **`MainWindowKeyboardShortcutKeyDispatchShellBridge`** — root **`KeyDown`** → **`KeyboardShortcutService.TryHandleKeyDown`** with modifier bitmask only ([Slice 38 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE38_MAINWINDOW_KEYBOARD_KEY_DISPATCH_SHELL.md)); not **Slice 36/37** **`RegisterShortcut`** registration, not **Slice 21** Help **`ContentDialog`** flow. **Forbidden:** calling **`RegisterShortcut`** or referencing **`MainWindowKeyboardShortcutRegistrationShellBridge`** in this file (creep guard).
- **`MainWindowStartupWelcomeActivationShellBridge`** — **First `Window.Activated`** path only: **`Content.KeyDown`** attach + smoke/safe gates + one-shot **`WelcomeView`** ([Slice 11 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE11_MAINWINDOW_STARTUP_WELCOME_ACTIVATION.md)); not Loaded bootstrap/tail, not workflow, not palette/catalog/toolbar/search. **Forbidden:** any second shell feature in this file without a new bounded brief + tests.
- **`MainWindowWindowActivatedLoggingShellBridge`** — **`Window.Activated`** **try/catch** + **`ErrorLogger.LogWarning`** with scope **`MainWindow.MainWindow_Activated`** only ([Slice 40 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE40_MAINWINDOW_WINDOW_ACTIVATED_LOGGING_SHELL.md)); **`RunActivatedAsync(Func<Task>)`** forwards to **`MainWindowStartupWelcomeActivationShellBridge.HandleActivatedAsync`**. **Forbidden:** **`HandleActivatedAsync`** / **`WelcomeView`** orchestration (**Slice 11**), **Slice 39** smoke/safe predicates, **Slice 38** **`KeyDown`** dispatch, shortcut **registration** (**Slices 36–37**).
- **`MainWindowJumpListTaskbarProgressShellBridge`** — **Loaded-bootstrap** jump list schedule + taskbar **`HWND`** for progress only ([Slice 12 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE12_MAINWINDOW_JUMPLIST_TASKBAR_PROGRESS_SHELL.md)); not pending jump-list dispatch (**Slice 15** — **`MainWindowJumpListDispatchShellBridge`**), not pending **file** activation (**Slice 14** — **`MainWindowFileActivationShellBridge`**), not notification center (**Slice 16** — **`MainWindowNotificationCenterShellBridge`**), not status strip clock (**Slice 17** — **`MainWindowStatusStripClockShellBridge`**), not status strip metrics (**Slice 18** — **`MainWindowStatusStripMetricsShellBridge`**), not status bar coordinator shell (**Slice 19** — **`MainWindowStatusBarCoordinatorShellBridge`**), not menu/tool activation shell (**Slice 20** — **`MainWindowMenuToolActivationShellBridge`**), not keyboard shortcuts shell (**Slice 21** — **`MainWindowKeyboardShortcutsShellBridge`**), not workflow. **Forbidden:** coordinator dispatch, **`FileActivation`**, **`JumpListActivation.TryConsumePending`**, **`WireNotificationCenter`** / notification center shell (**Slice 16**), **`MainWindowStatusStripClockShellBridge`**, **`MainWindowStatusStripMetricsShellBridge`**, **`MainWindowStatusBarCoordinatorShellBridge`**, **`MainWindowMenuToolActivationShellBridge`**, **`MainWindowKeyboardShortcutsShellBridge`**, or any other shell feature in this file without a new bounded brief + tests.
- **`MainWindowJumpListDispatchShellBridge`** — **`TryDispatchPendingJumpListActivation`** + **`RunJumpListPendingAsync`** only ([Slice 15 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE15_MAINWINDOW_JUMPLIST_DISPATCH_SHELL.md)); not **`WireJumpList`** / **`SetWindowHandle`** (Slice 12), not file activation, not notification center, not lifetime cleanup. **Forbidden:** absorbing **`MainWindowJumpListTaskbarProgressShellBridge`**, **`MainWindowFileActivationShellBridge`**, or any second shell feature without brief amendment.
- **`MainWindowFileActivationShellBridge`** — **`TryDispatchPendingFileActivation`** + **`RunFileActivationPendingAsync`** only ([Slice 14 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE14_MAINWINDOW_FILE_ACTIVATION_SHELL.md)); not jump-list dispatch, not notification center wire, not lifetime cleanup. **Forbidden:** absorbing **`TryDispatchPendingJumpListActivation`**, **`MainWindowNotificationCenterShellBridge`**, or any second shell feature without brief amendment.
- **`MainWindowLifetimeCleanupShellBridge`** — **`Window.Closed`** prelude + idempotent **`RunCleanupCore`** teardown only ([Slice 13 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE13_MAINWINDOW_LIFETIME_CLEANUP_SHELL.md)); not **`Activated`** / welcome, not Loaded bootstrap/tail, not pending jump-list dispatch (**Slice 15**), not pending file dispatch (**Slice 14**), not notification center **implementation** (**Slice 16** — channel may invoke **`CleanupNotificationCenter`** lambda only). **Forbidden:** workflow, palette, catalog, toolbar/search bridges, or any second shell feature in this file without a new bounded brief + tests.
- **`MainWindowNotificationCenterShellBridge`** — **`WireNotificationCenter`** + VM **`PropertyChanged`** badge path + **`CleanupNotificationCenter`** + command-forward entry points only ([Slice 16 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE16_MAINWINDOW_NOTIFICATION_CENTER_SHELL.md)); not file/jump-list activation, not Slice 12 **`WireJumpList`**, not startup welcome, not palette/catalog/toolbar/search. **Forbidden:** absorbing **`MainWindowFileActivationShellBridge`**, **`MainWindowJumpListDispatchShellBridge`**, or any second shell feature without brief amendment.
- **`MainWindowStatusStripClockShellBridge`** — **`ClockText`** one-minute wall-clock timer + dispose only ([Slice 17 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE17_MAINWINDOW_STATUS_STRIP_CLOCK_SHELL.md)); not **`MainWindowStatusStripMetricsShellBridge`**, not **`StatusBarCoordinator`**, not **`GlobalTransportControl`**, not notification center. **Forbidden:** CPU/GPU/RAM/latency metrics **`DispatcherTimer`**, transport play/stop, or any second shell feature without brief amendment.
- **`MainWindowStatusStripMetricsShellBridge`** — 2s **`DispatcherTimer`** + **`OnMetricsTick`** + **`UpdateGpuAndLatencyAsync`** for **Cpu/Gpu/Ram/Latency** text only ([Slice 18 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE18_MAINWINDOW_STATUS_STRIP_METRICS_SHELL.md)); not **`ClockText`**, not **`StatusBarCoordinator`** implementation or duplicate wiring, not **`MainWindowStatusBarCoordinatorShellBridge`**, not **`GlobalTransportControl`**, not notification center. **Forbidden:** wall-clock timer, transport play/stop, or any second shell feature without brief amendment.
- **`MainWindowStatusBarCoordinatorShellBridge`** — **`StatusBarCoordinator`** resolve + **`Attach`** + **`Subscribe`** + prelude **`StartBackendHealthMonitoring(StatusBarCoordinator?)`** only ([Slice 19 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE19_MAINWINDOW_STATUS_BAR_COORDINATOR_SHELL.md)); not **`MainWindowStatusStripClockShellBridge`**, not **`MainWindowStatusStripMetricsShellBridge`**, not duplicating **`StatusBarCoordinator`** handlers or **`Unsubscribe`** (lifetime channel on **`MainWindow`**). **Forbidden:** metrics/clock timers, notification center, menu/tool activation glue, or any second shell feature without brief amendment.
- **`MainWindowMenuToolActivationShellBridge`** — bounded **menu/tool activation** cluster only ([Slice 20 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE20_MAINWINDOW_MENU_TOOL_ACTIVATION_SHELL.md)): check-for-updates, mini-timeline toggle, collaboration panel toggle/hide, manage workspaces; not **`KeyboardShortcutsMenuItem_Click`** (**Slice 21** — **`MainWindowKeyboardShortcutsShellBridge`**), not toolbar customization (**Slice 7**), not command palette / catalog / search / status / startup retry / **`RHVoice`**. **Forbidden:** accretion of unrelated menu handlers or keyboard-shortcuts dialog stack into this bridge (same **distributed god-object** risk called out for **Slice 19** coordinator shell — do not route unrelated shell features here).
- **`MainWindowKeyboardShortcutsShellBridge`** — **Help → Keyboard shortcuts** dialog stack only ([Slice 21 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE21_MAINWINDOW_KEYBOARD_SHORTCUTS_SHELL.md)): shortcuts view + customize view + VM dispose + error toast; not **`MainWindowMenuToolActivationShellBridge`** (**Slice 20**), not toolbar customization (**Slice 7**), not command palette / catalog / search / status / **`RHVoice`**. **Forbidden:** unrelated menu handlers, transport, or status logic in this file without a new bounded brief + tests.
- **`MainWindowPanelPreviewShellBridge`** — **Nav rail** **`PointerEntered` / `Exited`** → **`PanelPreviewPopup`** + static preview text only ([Slice 23 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE23_MAINWINDOW_PANEL_PREVIEW_SHELL.md)); not **`ExecuteNavCommand`**, not **`MainWindowNavigationShellBridge`** implementation, not panel **quick switch** UI, not search / palette / catalog. **Forbidden:** navigation execution, `OpenPanelByIdAsync`, or any second shell feature in this file without brief amendment.
- **`MainWindowPanelQuickSwitchShellBridge`** — **Panel quick-switch** **IDEA 1** **`Popup` / `PanelQuickSwitchIndicator` / `DispatcherTimer`** only ([Slice 24 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE24_MAINWINDOW_PANEL_QUICK_SWITCH_INDICATOR_SHELL.md)); not **nav `PanelPreviewPopup`**, not **`OpenPanelByIdAsync`** / **`SwitchToPanel` panel assignment** (stays **MainWindow** + **`ShellNavigationCoordinator`**, smoke-gated as today), not keyboard shortcut registration bulk. **Forbidden:** merging with **`MainWindowPanelPreviewShellBridge`**, navigation-shell internals, or any second shell feature in this file without brief amendment.
- **`MainWindowPanelRegionFocusShellBridge`** — **Panel region focus + Ctrl+Tab cycling** only ([Slice 27 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE27_MAINWINDOW_PANEL_REGION_FOCUS_SHELL.md)); **`Func<PanelHost?>`** per region + **injected** quick-switch **label** callback only — **not** **Popup**/**DispatcherTimer** ownership (Slice 24), not **Ctrl+1–9** quick-switch table (**`RegisterPanelQuickSwitchShortcut`** stays **MainWindow**), not **dock** (**Slice 25**), not **nav preview** (**Slice 23**). **Forbidden:** naming **`MainWindowPanelQuickSwitchShellBridge`** in this file, or any second shell feature without brief amendment.
- **`MainWindowPanelDockShellBridge`** — **Panel dock / region swap (IDEA 14)** only ([Slice 25 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE25_MAINWINDOW_PANEL_DOCKING_SHELL.md)); injected **`getPanelHostByRegion`**, **`OpenPanelByIdAsync`**, **`PanelStateService`**, layout save, toast. **Not** nav preview, not quick-switch popup, not **`MainWindowNavigationShellBridge`**. **Forbidden:** search, palette, workflow, or a second shell feature in this file without brief amendment.
- **`MainWindowStartupOverlayShellBridge`** — **Backend startup overlay** only ([Slice 26 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE26_MAINWINDOW_STARTUP_OVERLAY_SHELL.md)); **`IStartupStateService`**, **`DispatcherQueue`**, **`StartupRetryCoordinator`** via constructor **`Func`**. **Not** **`MainWindowStartupWelcomeActivationShellBridge`** (welcome/activated one-shot), not panel/nav/search, not `ColdStartTimingCollector` ownership ( **`MainWindow`** passes one **`Action`**) . **Forbidden:** accreting panel dock, file activation, or any second shell feature without brief amendment.
- **`MainWindowHelpAboutShellBridge`** — **Help** → local **docs** in Explorer + **About** `ContentDialog` only ([Slice 28 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE28_MAINWINDOW_HELP_ABOUT_SHELL.md)); not **Edit** Undo/Redo (**Slice 29**), not **IBackendClient**. **Forbidden:** `ExecuteUndo` / `ExecuteRedo` or transport in this file.
- **`MainWindowEditUndoRedoShellBridge`** — **Edit** **Undo** / **Redo** (menu + **`edit.undo`** / **`edit.redo`** routes) → **`UndoRedoService`** + error log only ([Slice 29 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE29_MAINWINDOW_EDIT_UNDO_REDO_SHELL.md)); not **Help/About** (**Slice 28**), not **`MainWindowKeyboardShortcutsShellBridge`** **dialog** stack (**Slice 21**), not **bulk** of **`RegisterKeyboardShortcuts`**. **Forbidden:** new shortcut IDs or unrelated edit features without a new bounded brief + tests.
- **`MainWindowImportWorkflowShellBridge`** — **Import audio** entry from shell only ([Slice 31 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE31_MAINWINDOW_IMPORT_WORKFLOW_SHELL.md)): **`IStartupStateService`** ready gate, resolve **`IImportWorkflowService`**, **`ImportAudioFileAsync`** with **HWND**; not **`MainWindowGlobalTransportShellBridge`** (**Slice 30**), not **`IImportWorkflowService`** implementation, not project New/Open/Save (**Slice 4**). **Forbidden:** transport, undo, or project workflow in this file without a new bounded brief + tests.
- **`MainWindowShellChromeShellBridge`** — **Mica/acrylic**, **custom title bar** (`SetTitleBar`, `ExtendsContentIntoTitleBar`), and **`AppWindow.TitleBar`** colors + **`IUnifiedThemeService.ThemeChanged`** only ([Slice 32 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE32_MAINWINDOW_SHELL_CHROME_MICA_TITLEBAR.md)); not **Loaded** bootstrap/tail, not **import/transport/undo/palette**; **unsubscribe** is **idempotent** when no handler. **Forbidden:** accreting `MainWindowShellLoadedBootstrap` or workflow/panel logic without new bounded brief + tests.
- **`MainWindowWorkspaceSplitterShellBridge`** — **Workspace grid splitter** pointer handlers only ([Slice 33 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE33_MAINWINDOW_WORKSPACE_GRID_SPLITTER_SHELL.md)): **`FindNameOnContent`** for **`WorkspaceGrid`** / column+row definitions, **`CapturePointer` / `ReleasePointerCapture`**, star **GridLength** updates, **`Action`** to debounced **`SaveWorkspaceLayout`** on release; not **Mica**/**title bar** (**Slice 32**), not **import** (**Slice 31**), not **`InitializePanelsAsync`** / profile restore. **Forbidden:** accreting panel dock (**Slice 25**), region focus (**Slice 27**), or workspace soup without new bounded brief + tests.
- **`MainWindowMenuBarShellBridge`** — **Top-level** **`MenuBar`** build only ([Slice 34 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE34_MAINWINDOW_MENU_BAR_BUILD_SHELL.md)): **MenuBarHost** + **`IPanelRegistry`** for **Modules**; **`CommandRouter?`** for **`WireMenuItem`**; **`MainWindowMenuBarCommandCallbacks`** for host actions; not **Slice 22** **Recent Projects** **flyout population** (that stays **`MainWindowRecentProjectsMenuPopulationShellBridge`** + Loaded **`PopulateRecentProjectsMenu`**), not **Slice 20** / **21** / **import** / **splitter** / **chrome**. **Forbidden:** a second shell feature in this file without a new bounded brief + tests.

### Bounded seam owners (not extension buckets) — Task 348

These types are **bounded seam owners**. They are **not** extension buckets for unrelated shell behavior. If a change does not match the slice brief, it does **not** go into an existing bridge.

**Standing review checklist (PR / handoff):**

1. Does this PR add behavior to an existing bridge **without** a new bounded brief + tests? → **Reject** or split into a new slice.
2. Does a new or edited type reference **two unrelated** shell features (e.g. palette + catalog in one file)? → **Reject** or split seams.
3. Does **`MainWindow`** regain inline logic that belongs in a **chartered** bridge? → **Reject** unless the brief explicitly moves it back.
4. Does the widened **GAP-008 `dotnet test` filter** (canonical list in [`tools/gap008_mainwindow_regression_filter.txt`](../tools/gap008_mainwindow_regression_filter.txt)) shrink or drop prior slice test tokens? → **Reject** — extend only; keep regression spine.
5. Does the change touch **`engines/audio/rhvoice/`** during a GAP-008 shell batch without operator proof? → **Reject** (RHVoice frozen in lane).
6. Does the change copy **`StatusBarCoordinator`**’s **`IStartupStateService.StateChanged` → `UpdateActivityIndicators`** pattern for **unrelated** shell UI? → **Reject** — that wiring is **status primary-line truth only** ([startup truth brief](VOICESTUDIO_BOUNDED_GAP008_STARTUP_TRUTH_RECOVERY.md) § Operator proof); not a generic broadcast-refresh architecture.

**Owner list (GAP-008 shell seams):** Loaded bootstrap (`MainWindowShellLoadedBootstrap`); navigation shell (`MainWindowNavigationShellBridge` + `NavButtonActionSink`); nav panel preview on hover (`MainWindowPanelPreviewShellBridge`); panel quick-switch **indicator** (`MainWindowPanelQuickSwitchShellBridge`); **panel quick-switch shortcut registration** (`MainWindowPanelQuickSwitchShortcutRegistrationShellBridge`); panel **region focus + cycle** (`MainWindowPanelRegionFocusShellBridge`); panel dock / cross-region swap (`MainWindowPanelDockShellBridge`); **workspace grid splitters** (`MainWindowWorkspaceSplitterShellBridge`); **menu bar build (top-level MenuBar)** (`MainWindowMenuBarShellBridge`); **startup backend overlay** (`MainWindowStartupOverlayShellBridge`); Loaded tail (`MainWindowLoadedTailBootstrap` / `MainWindowLoadedTailHooks`); project workflow (`MainWindowProjectWorkflowBridge`); recent-project mutation (`MainWindowRecentProjectsMutationBridge`); search overlay (`MainWindowSearchOverlayShellBridge`); toolbar customization (`MainWindowToolbarCustomizationShellBridge`); command palette (`MainWindowCommandPaletteShellBridge`); toolbar command routing (`MainWindowToolbarCommandShellBridge`); tool catalog (`MainWindowToolCatalogShellBridge`); **tool catalog panel host chrome** (`MainWindowToolCatalogPanelHostChromeShellBridge`); **keyboard shortcut registration bulk** (`MainWindowKeyboardShortcutRegistrationShellBridge`); startup/welcome activation (`MainWindowStartupWelcomeActivationShellBridge`); jump list + taskbar progress shell (`MainWindowJumpListTaskbarProgressShellBridge`); jump list pending dispatch shell (`MainWindowJumpListDispatchShellBridge`); file activation shell (`MainWindowFileActivationShellBridge`); notification center shell (`MainWindowNotificationCenterShellBridge`); status strip clock shell (`MainWindowStatusStripClockShellBridge`); status strip metrics shell (`MainWindowStatusStripMetricsShellBridge`); status bar coordinator shell (`MainWindowStatusBarCoordinatorShellBridge`); menu / tool activation shell (`MainWindowMenuToolActivationShellBridge`); keyboard shortcuts shell (`MainWindowKeyboardShortcutsShellBridge`); Help / About shell (`MainWindowHelpAboutShellBridge`); **Edit — Undo/Redo** shell (`MainWindowEditUndoRedoShellBridge`); **import-audio / import workflow** shell (`MainWindowImportWorkflowShellBridge`); **global transport + timeline** shell (`MainWindowGlobalTransportShellBridge`); **shell chrome** (Mica, title bar, theme) (`MainWindowShellChromeShellBridge`); lifetime / shutdown cleanup shell (`MainWindowLifetimeCleanupShellBridge` + prelude/core channel records).

### Testing debt (GAP-008 / search overlay)

**Global search overlay collapse:** Real **`FrameworkElement.Visibility`** under a WinUI UI test host was **not** kept — host instability (see [Slice 6 brief — Follow-up: UI-host coverage](VOICESTUDIO_BOUNDED_GAP008_SLICE06_MAINWINDOW_SEARCH_OVERLAY_SHELL.md)). Unit tests + text pins remain the default gate until a stable UI harness or signed checklist closes the debt.

### Testing debt — DispatcherTimer / UI-host (status strip)

**Proven in-repo:** MSTest text pins + constructor / stop paths + canonical GAP-008 regression filter membership for **`MainWindowStatusStripClockShellBridge`**, **`MainWindowStatusStripMetricsShellBridge`**, and **`MainWindowStatusBarCoordinatorShellBridge`** (ordering vs **`BeginMetricsTimer`**, prelude hook for **`StartBackendHealthMonitoring`**).

**Not proven:** Full **`DispatcherTimer`** tick cadence and compositor timing under a **real WinUI host** (clock 1-minute interval, metrics 2-second interval).

**Closure criteria (when scheduled):** either a **stable UI harness** that drives the window and asserts observable text updates on tick boundaries, or a **signed operator checklist** that records wall-clock / metric strip behavior on a reference build. Until then, treat timer behavior as **integration / operator** truth — not implied by MSTest pins alone. [Slice 18 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE18_MAINWINDOW_STATUS_STRIP_METRICS_SHELL.md) §Testing debt cross-ref.

## GAP-008 Slice 1 — landed 2026-04-24

**Loaded shell bootstrap orchestration** moved to `MainWindowShellLoadedBootstrap.RunAsync` with `MainWindowLoadedBootstrapHooks` (see [VOICESTUDIO_BOUNDED_GAP008_SLICE01_MAINWINDOW_LOADED_BOOTSTRAP.md](VOICESTUDIO_BOUNDED_GAP008_SLICE01_MAINWINDOW_LOADED_BOOTSTRAP.md)). DEBUG diagnostics remain inline; **post-DEBUG tail** (`TransportShortcutCoordinator` attach + `RunPanelInitWhenReadyAsync` trigger) moved to **`MainWindowLoadedTailBootstrap`** — [Slice 3 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE03_MAINWINDOW_LOADED_TAIL.md).

## GAP-008 Slice 2 — landed 2026-04-25

**Navigation-shell glue** moved to **`MainWindowNavigationShellBridge`** + **`NavButtonActionSink`** (rail toggles, `INavigationService` subscribe/unsubscribe, nav command forwards). **`MainWindow`** keeps one-line forwards for partial call sites. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE02_MAINWINDOW_NAVIGATION_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE02_MAINWINDOW_NAVIGATION_SHELL.md); seam tests **`Gap008Slice2Tests`**.

## GAP-008 Slice 3 — landed 2026-04-25

**Loaded tail** (`TransportShortcutCoordinator` resolve + **`Attach`** + **`RunPanelInitWhenReadyAsync`**) orchestrated via **`MainWindowLoadedTailBootstrap.Run`** + **`MainWindowLoadedTailHooks`**; hooks close over `MainWindow` (no service locator inside runner). Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE03_MAINWINDOW_LOADED_TAIL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE03_MAINWINDOW_LOADED_TAIL.md); seam tests **`Gap008Slice3Tests`**.

## GAP-008 Slice 4 — landed 2026-04-25

**Project workflow entry points** (Choice A): **`MainWindowProjectWorkflowBridge`** forwards New/Open/Save/Open-recent to **`IProjectWorkflowCoordinator`**. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE04_MAINWINDOW_PROJECT_WORKFLOW.md](VOICESTUDIO_BOUNDED_GAP008_SLICE04_MAINWINDOW_PROJECT_WORKFLOW.md); tests **`Gap008Slice4Tests`** (3) + **`MainWindowProjectWorkflowBridgeTests`** (6).

## GAP-008 Slice 5 — landed 2026-04-25

**Recent-project mutations** (Option A): **`MainWindowRecentProjectsMutationBridge`** + **`IRecentProjectsMutationCommands`** — Pin, Unpin, Clear, Remove-from-list + toasts (Remove unchanged: no try/catch, no success toast). **`PopulateRecentProjectsMenu`** remains on **`MainWindow`** (composition only; click handlers forward to the bridge). Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE05_MAINWINDOW_RECENT_PROJECTS_MUTATION.md](VOICESTUDIO_BOUNDED_GAP008_SLICE05_MAINWINDOW_RECENT_PROJECTS_MUTATION.md); tests **`MainWindowRecentProjectsMutationBridgeTests`** (7) + **`Gap008Slice5Tests`** (4).

## GAP-008 Slice 6 — landed 2026-04-24

**Global search overlay shell glue:** **`MainWindowSearchOverlayShellBridge`** — Ctrl+K / menu **`ShowGlobalSearch`**, **`NavigateRequested`** forward, **`GlobalSearchOverlay_Tapped`** dismiss rule, ctor **`EnsureGlobalSearchOverlayCollapsed`**. **`SearchOverlayCoordinator`** unchanged as brain. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE06_MAINWINDOW_SEARCH_OVERLAY_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE06_MAINWINDOW_SEARCH_OVERLAY_SHELL.md); tests **`MainWindowSearchOverlayShellBridgeTests`** + **`Gap008Slice6Tests`**.

## GAP-008 Slice 7 — landed 2026-04-25

**Toolbar customization shell glue:** **`MainWindowToolbarCustomizationShellBridge`** — **`CustomizeToolbarMenuItem_Click`** forwards to **`ShowCustomizationDialogAsync`**; production launcher **`ToolbarCustomizationDialogLauncher`** wraps **`ToolbarCustomizationDialog`**. Command palette (**`ShowCommandPalette`**) and tool catalog (**`ShowToolCatalogAsync`**) were deferred to Slice 8+ (palette landed Slice 8). Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE07_MAINWINDOW_TOOLBAR_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE07_MAINWINDOW_TOOLBAR_SHELL.md); tests **`MainWindowToolbarCustomizationShellBridgeTests`** + **`Gap008Slice7Tests`**.

## GAP-008 Slice 8 — landed 2026-04-25

**Command palette shell glue:** **`MainWindowCommandPaletteShellBridge`** — **`ShowCommandPalette`** and Ctrl+P (**`nav.commandpalette`**) forward to **`Show()`**; production launcher **`CommandPaletteShellLauncher`** wraps **`CommandPaletteService`**. Failure path uses **`ICommandPaletteShellDiagnostics`** (default → **`ErrorLogger`**) + **`IToastNotificationService.ShowError`**. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE08_MAINWINDOW_COMMAND_PALETTE_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE08_MAINWINDOW_COMMAND_PALETTE_SHELL.md); tests **`MainWindowCommandPaletteShellBridgeTests`** + **`Gap008Slice8Tests`**.

## GAP-008 Slice 9 — landed 2026-04-25

**Toolbar command routing shell glue:** **`MainWindowToolbarCommandShellBridge`** + **`IToolbarShellImportFromToolbar`** — **`ToolbarViewModel`** **`import_audio`** → **`RequestImportAudio()`**; **`MainWindow`** wires **`ImportAudioFile`** via **`WireImportAudioHandler`**; **`CustomizableToolbar`** no longer uses **`App.MainWindowInstance`**. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE09_MAINWINDOW_TOOLBAR_COMMAND_ROUTING_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE09_MAINWINDOW_TOOLBAR_COMMAND_ROUTING_SHELL.md); tests **`MainWindowToolbarCommandShellBridgeTests`** + **`Gap008Slice9Tests`** + **`ToolbarViewModelTests`** (import delegation).

## GAP-008 Slice 10 — landed 2026-04-25

**Tool catalog shell glue:** **`MainWindowToolCatalogShellBridge`** + **`IToolCatalogShellLauncher`** + **`ToolCatalogShellLauncher`** + **`IToolCatalogShellDiagnostics`** — **`ShowToolCatalogAsync`** / **`nav.toolcatalog`** → dialog; **`WireToolCatalogHandlers`** supplies **`OpenPanelByIdAsync`** forward + **`PanelHost`** chrome; **`MainWindowToolCatalogShellBridgeTests`** + **`Gap008Slice10Tests`**. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE10_MAINWINDOW_TOOL_CATALOG_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE10_MAINWINDOW_TOOL_CATALOG_SHELL.md).

## GAP-008 Slice 40 — landed 2026-04-27

**Window-activated exception logging shell:** **`MainWindowWindowActivatedLoggingShellBridge`** — **`MainWindow_Activated`** **try/catch** + **`ErrorLogger.LogWarning`** removed from **`MainWindow`**; thin **`RunActivatedAsync(() => _startupWelcomeActivationShellBridge.HandleActivatedAsync(...))`**; **`Gap008Slice40Tests`** + **`MainWindowWindowActivatedLoggingShellBridgeTests`**; prepend-only [filter](../../tools/gap008_mainwindow_regression_filter.txt); full spine **N=302** per [reconciliation](../reports/verification/GAP008_MAINWINDOW_SPINE_COUNT_RECONCILIATION.md) § *Spine size after Slice 40*. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE40_MAINWINDOW_WINDOW_ACTIVATED_LOGGING_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE40_MAINWINDOW_WINDOW_ACTIVATED_LOGGING_SHELL.md). **Tasks 221–230**.

## GAP-008 Slice 41 — landed 2026-04-27

**Keyboard shortcuts menu item wiring shell:** **`MainWindowKeyboardShortcutsMenuItemShellBridge`** — **`KeyboardShortcutsMenuItem_Click`** **removed** from **`MainWindow`**; **`MenuFlyoutItem.Click`** + **`MainWindowKeyboardShortcutRegistrationDependencies`** “show keyboard shortcuts” callback forward to **`MainWindowKeyboardShortcutsShellBridge.RunKeyboardShortcutsMenuFlowAsync`** (**Slice 21** unchanged). **`Gap008Slice41Tests`** + **`MainWindowKeyboardShortcutsMenuItemShellBridgeTests`**; **`Gap008Slice21Tests`** / **`Gap008Slice20Tests`** pins updated; prepend-only [filter](../../tools/gap008_mainwindow_regression_filter.txt); full spine **N=311** per [reconciliation](../reports/verification/GAP008_MAINWINDOW_SPINE_COUNT_RECONCILIATION.md) § *Spine size after Slice 41*. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE41_MAINWINDOW_KEYBOARD_SHORTCUTS_MENU_ITEM_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE41_MAINWINDOW_KEYBOARD_SHORTCUTS_MENU_ITEM_SHELL.md). **Tasks 231–240**.

## GAP-008 Slice 39 — landed 2026-04-27

**Smoke / safe-startup mode shell:** **`MainWindowSmokeStartupModeShellBridge`** — **`IsSafeStartupMode` / `IsGateCSmokeMode`** removed from **`MainWindow`**; **`ShellNavigationCoordinator`** duplicate safe-mode probe removed in favor of **`EvaluateSafeStartup()`**; **`Gap008Slice39Tests`** + **`MainWindowSmokeStartupModeShellBridgeTests`**; prepend-only [filter](../../tools/gap008_mainwindow_regression_filter.txt); full spine **N=293** per [reconciliation](../reports/verification/GAP008_MAINWINDOW_SPINE_COUNT_RECONCILIATION.md) § *Spine size after Slice 39* (**superseded** for spine **N** by **Slice 40** → **302**). Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE39_MAINWINDOW_SMOKE_STARTUP_MODE_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE39_MAINWINDOW_SMOKE_STARTUP_MODE_SHELL.md). **Tasks 211–220**.

## Next Slice: Slice 42+ (Slice 41 landed; **Accepted** `VOICESTUDIO_BOUNDED_GAP008_SLICE42_*.md` before new **`MainWindow*ShellBridge`** code)

**2026-04-27 (Tasks 231–240 — execution):** **Slice 41** **landed** — [VOICESTUDIO_BOUNDED_GAP008_SLICE41_MAINWINDOW_KEYBOARD_SHORTCUTS_MENU_ITEM_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE41_MAINWINDOW_KEYBOARD_SHORTCUTS_MENU_ITEM_SHELL.md) **`MainWindowKeyboardShortcutsMenuItemShellBridge`**; full spine **N=311** per [reconciliation](../reports/verification/GAP008_MAINWINDOW_SPINE_COUNT_RECONCILIATION.md) § *Spine size after Slice 41*. **GAP-008 continues with Slice 42+** — **Accepted** next bounded brief before additional **`MainWindow*ShellBridge`** code. **Path G1** only; not umbrella close.

**2026-04-27 (Tasks 221–230 — execution):** **Slice 40** **landed** — [VOICESTUDIO_BOUNDED_GAP008_SLICE40_MAINWINDOW_WINDOW_ACTIVATED_LOGGING_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE40_MAINWINDOW_WINDOW_ACTIVATED_LOGGING_SHELL.md) **`MainWindowWindowActivatedLoggingShellBridge`**; **`MainWindow`** **`MainWindow_Activated`** inline **try/catch** **removed**; full spine **N=302** at land per [reconciliation](../reports/verification/GAP008_MAINWINDOW_SPINE_COUNT_RECONCILIATION.md) § *Spine size after Slice 40* (**superseded** for spine **N** by **Slice 41** → **311**). **Superseded** for “next slice” by **Slice 41** row above.

**2026-04-27 (Tasks 211–220 — execution):** **Slice 39** **landed** — [VOICESTUDIO_BOUNDED_GAP008_SLICE39_MAINWINDOW_SMOKE_STARTUP_MODE_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE39_MAINWINDOW_SMOKE_STARTUP_MODE_SHELL.md) **`MainWindowSmokeStartupModeShellBridge`**; **`MainWindow`** static smoke/safe probes **removed**; full spine **N=293** at land per [reconciliation](../reports/verification/GAP008_MAINWINDOW_SPINE_COUNT_RECONCILIATION.md) § *Spine size after Slice 39* (**superseded** for spine **N** by **Slice 40** → **302**). **Superseded** for “next slice” by **Slice 40** row above.

**2026-04-26 (Tasks 201–210 — execution):** **Slice 38** **landed** — [VOICESTUDIO_BOUNDED_GAP008_SLICE38_MAINWINDOW_KEYBOARD_KEY_DISPATCH_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE38_MAINWINDOW_KEYBOARD_KEY_DISPATCH_SHELL.md) **`MainWindowKeyboardShortcutKeyDispatchShellBridge`**; **`MainWindow`** **`MainWindow_KeyDown`** modifier assembly **removed** (thin forward); full spine **N=282** at land per [reconciliation](../reports/verification/GAP008_MAINWINDOW_SPINE_COUNT_RECONCILIATION.md) § *Spine size after Slice 38* (**superseded** for spine **N** by **Slice 39** → **293**). **Superseded** for “next slice” by **Slice 39** row above.

**2026-04-26 (Tasks 193–200 — execution):** **Slice 37** **landed** — [VOICESTUDIO_BOUNDED_GAP008_SLICE37_MAINWINDOW_PANEL_QUICK_SWITCH_SHORTCUT_REGISTRATION_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE37_MAINWINDOW_PANEL_QUICK_SWITCH_SHORTCUT_REGISTRATION_SHELL.md) **`MainWindowPanelQuickSwitchShortcutRegistrationShellBridge`**; **`MainWindow`** **`private void RegisterPanelQuickSwitchShortcut`** **removed**; **`MainWindowKeyboardShortcutRegistrationDependencies`** no **`registerPanelQuickSwitchGroup`**; full spine **N=275** per [reconciliation](../reports/verification/GAP008_MAINWINDOW_SPINE_COUNT_RECONCILIATION.md) § *Spine size after Slice 37*. **Superseded** for “next slice” by **Slice 38** row above.

**2026-04-26 (Tasks 179–188 — execution):** **Slice 36** **landed** — [VOICESTUDIO_BOUNDED_GAP008_SLICE36_MAINWINDOW_KEYBOARD_SHORTCUT_REGISTRATION_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE36_MAINWINDOW_KEYBOARD_SHORTCUT_REGISTRATION_SHELL.md) **`MainWindowKeyboardShortcutRegistrationShellBridge`**; **`MainWindow`** **`private void RegisterKeyboardShortcuts`** **removed**; **Ctrl+1–9** registration **moved** to **Slice 37** (above); full spine **N=268** at Slice 36 land (superseded by **Slice 37** → **N=275**).

**2026-04-26 (Tasks 169–178):** [Slice 35 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE35_MAINWINDOW_TOOL_CATALOG_PANEL_HOST_CHROME_SHELL.md) **landed** — **`MainWindowToolCatalogPanelHostChromeShellBridge`**; spine **N=262** (historical; superseded by **Slice 36** → **N=268**).

**2026-04-26 (Tasks 159–168):** [Slice 34 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE34_MAINWINDOW_MENU_BAR_BUILD_SHELL.md) **Accepted** and **landed** — **`MainWindowMenuBarShellBridge`** (top-level **`MenuBar`**, Modules from **`IPanelRegistry`**, **`MainWindow.Menu.cs`** removed). **Superseded** for “next slice” by **Slice 35** land + **Slice 36** rows above.

**2026-04-26 (Tasks 149–158):** [Slice 33 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE33_MAINWINDOW_WORKSPACE_GRID_SPLITTER_SHELL.md) **Accepted** and **landed** — **`MainWindowWorkspaceSplitterShellBridge`**. **Superseded** for “next slice” by **Slice 34** row above.

**2026-04-26 (Tasks 135–148):** [Slice 32 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE32_MAINWINDOW_SHELL_CHROME_MICA_TITLEBAR.md) **Accepted** and **landed** — **`MainWindowShellChromeShellBridge`**. **Superseded** for “next slice” by **Slice 33** row above.

**2026-04-26 (Tasks 124–133):** [Slice 31 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE31_MAINWINDOW_IMPORT_WORKFLOW_SHELL.md) **Accepted** and **landed** — **`MainWindowImportWorkflowShellBridge`** ( **`ImportAudioFile`**: ready gate, **`IImportWorkflowService`**, **HWND**). **Task 124** — `.\scripts\verify.ps1 -Quick` **exit 0**; **Report:** `artifacts/verify/20260426_132526/verification_report.md`. **Superseded** for “next slice” by **Slice 32** row above. **Task 134** (optional WinUI / runtime appendix) **not** a spine gate.

**2026-04-26 (Tasks 114–123):** [Slice 30 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE30_MAINWINDOW_GLOBAL_TRANSPORT_TIMELINE_SHELL.md) **Accepted** and **landed** — **`MainWindowGlobalTransportShellBridge`**. **Superseded** for “next slice” by **Slice 31** row above.

**2026-04-26 (Tasks 105–113):** [Slice 29 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE29_MAINWINDOW_EDIT_UNDO_REDO_SHELL.md) **Accepted** and **landed** — **`MainWindowEditUndoRedoShellBridge`**. **Superseded** for “next slice” by **Slice 30** / **31** rows above.

**Slice 31 landed** (2026-04-26): [VOICESTUDIO_BOUNDED_GAP008_SLICE31_MAINWINDOW_IMPORT_WORKFLOW_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE31_MAINWINDOW_IMPORT_WORKFLOW_SHELL.md) — **`MainWindowImportWorkflowShellBridge`**; **`Gap008Slice31Tests`** + **`MainWindowImportWorkflowShellBridgeTests`**; spine **N=237** in [reconciliation](../reports/verification/GAP008_MAINWINDOW_SPINE_COUNT_RECONCILIATION.md) § *Spine size after Slice 31* + filter + `last_run_summary.json`; TRX **`.buildlogs/gap008_spine/gap008_spine_20260426_133328.trx`**.

**Slice 30 landed** (2026-04-26): [VOICESTUDIO_BOUNDED_GAP008_SLICE30_MAINWINDOW_GLOBAL_TRANSPORT_TIMELINE_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE30_MAINWINDOW_GLOBAL_TRANSPORT_TIMELINE_SHELL.md) — **`MainWindowGlobalTransportShellBridge`**; **`Gap008Slice30Tests`** + **`MainWindowGlobalTransportShellBridgeTests`**; spine **N=231** (historical — superseded by **Slice 31** → **N=237**) in [reconciliation](../reports/verification/GAP008_MAINWINDOW_SPINE_COUNT_RECONCILIATION.md) § *Spine size after Slice 30* + filter + `last_run_summary.json`.

**Slice 29 landed** (2026-04-26): [VOICESTUDIO_BOUNDED_GAP008_SLICE29_MAINWINDOW_EDIT_UNDO_REDO_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE29_MAINWINDOW_EDIT_UNDO_REDO_SHELL.md) — **`MainWindowEditUndoRedoShellBridge`**; **`Gap008Slice29Tests`** + **`MainWindowEditUndoRedoShellBridgeTests`**; spine **N=224** (historical — superseded by **Slice 30** → **N=231**).

**Slice 28 landed** (2026-04-26): [VOICESTUDIO_BOUNDED_GAP008_SLICE28_MAINWINDOW_HELP_ABOUT_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE28_MAINWINDOW_HELP_ABOUT_SHELL.md) — **`MainWindowHelpAboutShellBridge`**; **`Gap008Slice28Tests`** + **`MainWindowHelpAboutShellBridgeTests`**; spine **N=217** (historical — superseded by **Slice 29** → **N=224**).

**Slice 27 landed** (2026-04-26): [Slice 27 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE27_MAINWINDOW_PANEL_REGION_FOCUS_SHELL.md) — **`MainWindowPanelRegionFocusShellBridge`**; **`Gap008Slice27Tests`** + **`MainWindowPanelRegionFocusShellBridgeTests`**; spine **N=212** (historical — superseded by **Slice 28** → **N=217** → **Slice 29** → **N=224**).

**2026-04-26 (Tasks 89–96 — post–verdict):** [VOICESTUDIO_RUNTIME_TRUTH_LANE_2026-04-26.md](../reports/verification/VOICESTUDIO_RUNTIME_TRUTH_LANE_2026-04-26.md) **§ Verdict** **partial**; **Task 94 = R1** — runtime gate for **charter-first** **Slice 28+** (satisfied for Slice 28; see **Slice 28** row above).

**Slice 25 landed** (2026-04-26): [Slice 25 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE25_MAINWINDOW_PANEL_DOCKING_SHELL.md) — **`MainWindowPanelDockShellBridge`** (IDEA 14); **`Gap008Slice25Tests`** + **`MainWindowPanelDockShellBridgeTests`**; **superseded** for spine by Slice 26 row above.

**Slice 20 landed** (2026-04-26): [Slice 20 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE20_MAINWINDOW_MENU_TOOL_ACTIVATION_SHELL.md) — **`MainWindowMenuToolActivationShellBridge`**; **`Gap008Slice20Tests`** + **`MainWindowMenuToolActivationShellBridgeTests`**; spine count superseded by later slices.

**Slice 19 landed** (2026-04-25): [Slice 19 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE19_MAINWINDOW_STATUS_BAR_COORDINATOR_SHELL.md) — **`MainWindowStatusBarCoordinatorShellBridge`**; **`Gap008Slice19Tests`** + **`MainWindowStatusBarCoordinatorShellBridgeTests`**; spine count superseded by Slice 20 row above.

**Slice 18 landed** (2026-04-25): [Slice 18 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE18_MAINWINDOW_STATUS_STRIP_METRICS_SHELL.md) — **`MainWindowStatusStripMetricsShellBridge`**; **`Gap008Slice18Tests`** + **`MainWindowStatusStripMetricsShellBridgeTests`**; **`MainWindow.StatusBar.cs`** removed.

**Slice 17 landed** (2026-04-25): [Slice 17 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE17_MAINWINDOW_STATUS_STRIP_CLOCK_SHELL.md) — **`MainWindowStatusStripClockShellBridge`**; **`Gap008Slice17Tests`** + **`MainWindowStatusStripClockShellBridgeTests`**. *(Metrics **`DispatcherTimer`** moved to Slice 18; **`MainWindow.StatusBar.cs`** removed.)*

**Slice 16 landed** (2026-04-26): [Slice 16 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE16_MAINWINDOW_NOTIFICATION_CENTER_SHELL.md) — **`MainWindowNotificationCenterShellBridge`**; **`Gap008Slice16Tests`** + **`MainWindowNotificationCenterShellBridgeTests`**.

**Slice 15 landed** (2026-04-26): [Slice 15 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE15_MAINWINDOW_JUMPLIST_DISPATCH_SHELL.md) — **`MainWindowJumpListDispatchShellBridge`**; **`Gap008Slice15Tests`** + **`MainWindowJumpListDispatchShellBridgeTests`**.

## GAP-008 Slice 11 — landed 2026-04-25

**Startup / welcome activation shell:** **`MainWindowStartupWelcomeActivationShellBridge`** — **`HandleActivatedAsync`** owns KeyDown attach, smoke/safe gates, **`WelcomeView`** one-shot + settings; **`MainWindow`** forwards **`Activated`** and passes **`MainWindow_KeyDown`**. Tests: **`Gap008Slice11Tests`** + **`MainWindowStartupWelcomeActivationShellBridgeTests`**. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE11_MAINWINDOW_STARTUP_WELCOME_ACTIVATION.md](VOICESTUDIO_BOUNDED_GAP008_SLICE11_MAINWINDOW_STARTUP_WELCOME_ACTIVATION.md).

## GAP-008 Slice 12 — landed 2026-04-25

**Jump list + taskbar progress shell:** **`MainWindowJumpListTaskbarProgressShellBridge`** — **`WireJumpList`**, **`WireTaskbarProgress`** ( **`Func<IntPtr>`** window handle at invoke time); **`MainWindowShellLoadedBootstrap`** hooks forward from **`MainWindow`**. Pending jump-list/file activation and notification center **out of scope** (see Slice 12 brief). Tests: **`Gap008Slice12Tests`** + **`MainWindowJumpListTaskbarProgressShellBridgeTests`**. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE12_MAINWINDOW_JUMPLIST_TASKBAR_PROGRESS_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE12_MAINWINDOW_JUMPLIST_TASKBAR_PROGRESS_SHELL.md).

## GAP-008 Slice 13 — landed 2026-04-25

**Lifetime / shutdown cleanup shell:** **`MainWindowLifetimeCleanupShellBridge`** — **`OnClosedPrelude`** (status timer stop, debouncer cancel, workspace save, clean-shutdown mark) + **`RunCleanupCore`** (idempotent teardown including temp-audio cleanup, service disposes, shell chrome unsubscribe); **`MainWindowClosedPreludeChannels`** / **`MainWindowLifetimeCleanupCoreChannels`** constructed in **`MainWindow`** ctor. Tests: **`Gap008Slice13Tests`** + **`MainWindowLifetimeCleanupShellBridgeTests`**. Regression spine: **[`scripts/Run-Gap008MainWindowRegressionTests.ps1`](../scripts/Run-Gap008MainWindowRegressionTests.ps1)**. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE13_MAINWINDOW_LIFETIME_CLEANUP_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE13_MAINWINDOW_LIFETIME_CLEANUP_SHELL.md).

## GAP-008 Slice 14 — landed 2026-04-25

**File activation shell:** **`MainWindowFileActivationShellBridge`** — **`TryDispatchPendingFileActivation`** + private **`RunFileActivationPendingAsync`** (**`FileActivation`**, **`IStartupStateService`** gate, **`IProjectWorkflowCoordinator`**, toasts, **`IShellNavigationCoordinator`** for profile path); **`MainWindow`** ctor wires **`Func`** accessors after coordinator creation; Loaded-bootstrap hook forwards **`TryDispatchPendingFileActivation`**. Jump-list **pending** dispatch → **`MainWindowJumpListDispatchShellBridge`** (Slice **15**). Tests: **`Gap008Slice14Tests`** + **`MainWindowFileActivationShellBridgeTests`**. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE14_MAINWINDOW_FILE_ACTIVATION_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE14_MAINWINDOW_FILE_ACTIVATION_SHELL.md).

## GAP-008 Slice 15 — landed 2026-04-26

**Jump list pending dispatch shell:** **`MainWindowJumpListDispatchShellBridge`** — **`TryDispatchPendingJumpListActivation`** + private **`RunJumpListPendingAsync`** (**`JumpListActivation`**, **`IStartupStateService`** gate, **`IProjectWorkflowCoordinator`**, toast on failure); **`MainWindow`** ctor after **`MainWindowFileActivationShellBridge`**; Loaded-bootstrap hook **`TryDispatchPendingJumpListActivation`** → **`() => _jumpListDispatchShellBridge.TryDispatchPendingJumpListActivation()`**. **Not** Slice 12 **`WireJumpList`** / **`WireTaskbarProgress`**. Tests: **`Gap008Slice15Tests`** + **`MainWindowJumpListDispatchShellBridgeTests`**. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE15_MAINWINDOW_JUMPLIST_DISPATCH_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE15_MAINWINDOW_JUMPLIST_DISPATCH_SHELL.md).

## GAP-008 Slice 16 — landed 2026-04-26

**Notification center shell:** **`MainWindowNotificationCenterShellBridge`** — **`WireNotificationCenter`**, VM **`PropertyChanged`** → badge, **`CleanupNotificationCenter`** (lifetime channel one-line forward), **`OnMarkAllReadClick`** / **`OnDismissItemClick`**; **`MainWindow`** ctor after **`MainWindowJumpListDispatchShellBridge`**; Loaded-bootstrap **`WireNotificationCenter`** → **`() => _notificationCenterShellBridge.WireNotificationCenter()`**. Tests: **`Gap008Slice16Tests`** + **`MainWindowNotificationCenterShellBridgeTests`**. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE16_MAINWINDOW_NOTIFICATION_CENTER_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE16_MAINWINDOW_NOTIFICATION_CENTER_SHELL.md).

## GAP-008 Slice 17 — landed 2026-04-25

**Status strip clock shell:** **`MainWindowStatusStripClockShellBridge`** — **`BeginClockTimer`**, **`RefreshClockText`**, **`DisposeClockTimer`** for **`ClockText`** one-minute wall clock; **`MainWindow`** Loaded tail; lifetime **`DisposeClockTimer`** one-line forward. *(Pre–Slice 18: metrics lived on **`MainWindow.StatusBar.cs`**; Slice 18 removed that partial — see Slice 18 row.)* Tests: **`Gap008Slice17Tests`** + **`MainWindowStatusStripClockShellBridgeTests`**. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE17_MAINWINDOW_STATUS_STRIP_CLOCK_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE17_MAINWINDOW_STATUS_STRIP_CLOCK_SHELL.md). Spine count authority: [GAP008_MAINWINDOW_SPINE_COUNT_RECONCILIATION.md](../reports/verification/GAP008_MAINWINDOW_SPINE_COUNT_RECONCILIATION.md).

## GAP-008 Slice 18 — landed 2026-04-25

**Status strip metrics shell:** **`MainWindowStatusStripMetricsShellBridge`** — **`BeginMetricsTimer`**, **`StopMetricsTimer`**, **`OnMetricsTick`**, **`UpdateGpuAndLatencyAsync`** for **Cpu/Gpu/Ram/Latency** text; **`MainWindow`** Loaded calls **`BeginMetricsTimer()`**; lifetime prelude **`StopStatusBarTimer`** → **`StopMetricsTimer()`**; **`MainWindow.StatusBar.cs`** removed. Tests: **`Gap008Slice18Tests`** + **`MainWindowStatusStripMetricsShellBridgeTests`**. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE18_MAINWINDOW_STATUS_STRIP_METRICS_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE18_MAINWINDOW_STATUS_STRIP_METRICS_SHELL.md).

## GAP-008 Slice 19 — landed 2026-04-25

**Status bar coordinator shell wiring:** **`MainWindowStatusBarCoordinatorShellBridge`** — **`ResolveAttachSubscribe`** (**`Attach`** + **`Subscribe`**) after **`BeginMetricsTimer()`**; Loaded-bootstrap **`StartBackendHealthMonitoring`** → **`StartBackendHealthMonitoring(_statusBarCoordinator)`**; lifetime **`UnsubscribeStatusBarCoordinator`** unchanged on **`MainWindow`**. Tests: **`Gap008Slice19Tests`** + **`MainWindowStatusBarCoordinatorShellBridgeTests`**. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE19_MAINWINDOW_STATUS_BAR_COORDINATOR_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE19_MAINWINDOW_STATUS_BAR_COORDINATOR_SHELL.md).

## GAP-008 Slice 20 — landed 2026-04-26

**Menu / tool activation shell:** **`MainWindowMenuToolActivationShellBridge`** — **`RunCheckForUpdatesAsync`**, **`RunToggleMiniTimelineAsync`**, **`ToggleCollaborationPanelVisibility`**, **`HideCollaborationPanel`**, **`RunManageWorkspacesAsync`**; **`MainWindow`** + **`MainWindow.Workspaces`** thin forwards; **`KeyboardShortcutsMenuItem_Click`** **DEFERRED** on **`MainWindow`** → **Slice 41** **`MainWindowKeyboardShortcutsMenuItemShellBridge`**. Tests: **`Gap008Slice20Tests`** + **`MainWindowMenuToolActivationShellBridgeTests`**. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_SLICE20_MAINWINDOW_MENU_TOOL_ACTIVATION_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE20_MAINWINDOW_MENU_TOOL_ACTIVATION_SHELL.md).

## GAP-008 Startup truth recovery — landed 2026-04-25

**Hotfix lane (not a numbered decomposition slice):** **`StatusBarCoordinator`** subscribes **`IStartupStateService.StateChanged`** and enqueues **`UpdateActivityIndicators`** on startup transitions; **`OnBackendReachabilityChanged`** merges reachability into **`ActivityStatusChangedEventArgs`** and calls **`UpdateActivityIndicators`** (no direct **`StatusText = "Ready"`** bypass). Tests: **`Gap008StartupTruthTests`**. Bounded brief: [VOICESTUDIO_BOUNDED_GAP008_STARTUP_TRUTH_RECOVERY.md](VOICESTUDIO_BOUNDED_GAP008_STARTUP_TRUTH_RECOVERY.md). Operator visual closure: [PROOF_GAP008_STARTUP_TRUTH_VISUAL_2026-04-25.md](../reports/verification/PROOF_GAP008_STARTUP_TRUTH_VISUAL_2026-04-25.md).

**Alternate candidate:** Remaining menu / tool activation glue not yet covered by GAP-008 bridges — **Slice 41** covers **Help → Keyboard Shortcuts** menu item wiring; further items (**Check for Updates**, **Customize Toolbar**, …) remain **one** bounded brief each when chartered.

### Future Slices (Not Yet Scoped)

| Slice | Description | Priority |
|-------|-------------|----------|
| Post–Slice 20 seams | **Slice 21+** — next chartered brief only (**one seam per brief**; no “remaining shell stuff” bucket) | Medium |
| Further MainWindow cuts | Additional seams only with new bounded brief | Low |

---

## Changelog

- 2026-04-27: **GAP-008 Slice 41** — `MainWindowKeyboardShortcutsMenuItemShellBridge` (keyboard shortcuts **menu item** + shortcut callback wiring → **Slice 21**); `Gap008Slice41Tests` + `MainWindowKeyboardShortcutsMenuItemShellBridgeTests`; filter prepend; spine **311**; brief [VOICESTUDIO_BOUNDED_GAP008_SLICE41_MAINWINDOW_KEYBOARD_SHORTCUTS_MENU_ITEM_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE41_MAINWINDOW_KEYBOARD_SHORTCUTS_MENU_ITEM_SHELL.md); **Tasks 231–240**; **Next:** **Slice 42+** — chartered brief only.
- 2026-04-27: **GAP-008 Slice 40** — `MainWindowWindowActivatedLoggingShellBridge` (**`MainWindow_Activated`** **try/catch** + **`ErrorLogger.LogWarning`**; thin forward to **`HandleActivatedAsync`**); `Gap008Slice40Tests` + `MainWindowWindowActivatedLoggingShellBridgeTests`; filter prepend; spine **302**; brief [VOICESTUDIO_BOUNDED_GAP008_SLICE40_MAINWINDOW_WINDOW_ACTIVATED_LOGGING_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE40_MAINWINDOW_WINDOW_ACTIVATED_LOGGING_SHELL.md); **Tasks 221–230**; **Next:** superseded by **Slice 41** charter row above.
- 2026-04-27: **GAP-008 Slice 39** — `MainWindowSmokeStartupModeShellBridge` (Gate-C smoke / **`VOICESTUDIO_SAFE_STARTUP`** probes; coordinator **`EvaluateSafeStartup`** dedupe); `Gap008Slice39Tests` + `MainWindowSmokeStartupModeShellBridgeTests`; filter prepend; spine **293**; brief [VOICESTUDIO_BOUNDED_GAP008_SLICE39_MAINWINDOW_SMOKE_STARTUP_MODE_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE39_MAINWINDOW_SMOKE_STARTUP_MODE_SHELL.md); **Tasks 211–220**; **Next:** superseded by **Slice 40** row above.
- 2026-04-26: **GAP-008 Slice 38** — `MainWindowKeyboardShortcutKeyDispatchShellBridge` (root **`KeyDown`** → **`TryHandleKeyDown`**); `Gap008Slice38Tests` + `MainWindowKeyboardShortcutKeyDispatchShellBridgeTests`; filter prepend; spine **282**; brief [VOICESTUDIO_BOUNDED_GAP008_SLICE38_MAINWINDOW_KEYBOARD_KEY_DISPATCH_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE38_MAINWINDOW_KEYBOARD_KEY_DISPATCH_SHELL.md); **Tasks 201–210**; **Next:** superseded by **Slice 39** row above.
- 2026-04-26: **GAP-008 Slice 30** — `MainWindowGlobalTransportShellBridge` (transport **Play/Stop**, `TransportShortcutCoordinator` **recording** panel, **ToggleRecording**, timeline **ZoomIn**/**ZoomOut**/**ResetZoom** + keyboard zoom routes); `Gap008Slice30Tests` + `MainWindowGlobalTransportShellBridgeTests`; filter prepend; spine **231**; brief [VOICESTUDIO_BOUNDED_GAP008_SLICE30_MAINWINDOW_GLOBAL_TRANSPORT_TIMELINE_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE30_MAINWINDOW_GLOBAL_TRANSPORT_TIMELINE_SHELL.md); **Tasks 114–123**; **Next:** **Slice 31+** — chartered brief only.
- 2026-04-26: **GAP-008 Slice 29** — `MainWindowEditUndoRedoShellBridge` (Edit **Undo/Redo** + **edit.undo** / **edit.redo**); `Gap008Slice29Tests` + `MainWindowEditUndoRedoShellBridgeTests`; filter prepend; spine **224**; brief [VOICESTUDIO_BOUNDED_GAP008_SLICE29_MAINWINDOW_EDIT_UNDO_REDO_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE29_MAINWINDOW_EDIT_UNDO_REDO_SHELL.md); **Tasks 105–113**; **Next:** superseded by **Slice 30** row above.
- 2026-04-26: **GAP-008 Slice 28** — `MainWindowHelpAboutShellBridge` (Help: **Documentation Folder** in Explorer, **About** `ContentDialog`); `Gap008Slice28Tests` + `MainWindowHelpAboutShellBridgeTests`; filter prepend; spine **217**; brief [VOICESTUDIO_BOUNDED_GAP008_SLICE28_MAINWINDOW_HELP_ABOUT_SHELL.md](VOICESTUDIO_BOUNDED_GAP008_SLICE28_MAINWINDOW_HELP_ABOUT_SHELL.md); **Next:** superseded by **Slice 29** row above.
- 2026-04-26: **Runtime-truth first pass** — [VOICESTUDIO_RUNTIME_TRUTH_LANE_2026-04-26.md](../reports/verification/VOICESTUDIO_RUNTIME_TRUTH_LANE_2026-04-26.md) **§ Verdict** **partial**; **Task 94** **R1**; **Slice 28+** unblocked for **charter-first** work ([STATE.md](../../.cursor/STATE.md)).
- 2026-04-26: **Tasks 89–96** — runtime-truth SSOT + prior **Slice 28** freeze (superseded by verdict row above); [STATE.md](../../.cursor/STATE.md) + tracker; **Task 94** was **PENDING** until pass.
- 2026-04-26: **Tasks 79–88 (GAP-008) — Slice 27** — `MainWindowPanelRegionFocusShellBridge` (panel region **Focus** + **Ctrl+Tab** / **Ctrl+Alt+1–4**; injected Slice 24 indicator callback; **no** `MainWindow` private `FocusPanelRegion`); `Gap008Slice27Tests` + `MainWindowPanelRegionFocusShellBridgeTests`; `Gap008Slice24Tests` pins updated for constructor order; Task **79** G1; filter prepend; spine **212** (206+6); [Slice 27 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE27_MAINWINDOW_PANEL_REGION_FOCUS_SHELL.md); **Next:** **Slice 28+** — chartered brief only. **Narrow-seam** row + owner list entry for panel region focus shell.
- 2026-04-26: **Tasks 69–78 (GAP-008) — Slice 26** — `MainWindowStartupOverlayShellBridge` (startup backend cold-start overlay + retry); `Gap008Slice26Tests` + `MainWindowStartupOverlayShellBridgeTests`; Task **69** G1 one-sentence truth; filter prepend; spine **206**; [Slice 26 brief](VOICESTUDIO_BOUNDED_GAP008_SLICE26_MAINWINDOW_STARTUP_OVERLAY_SHELL.md); **Next:** superseded by **Slice 27** row above.
- 2026-04-26: **Tasks 449–458** — **Slice 20** — `MainWindowMenuToolActivationShellBridge`; `Gap008Slice20Tests` + `MainWindowMenuToolActivationShellBridgeTests`; filter superset; spine **161**; CI coherence check `listedTestCount` vs `passed` ([`tests/ci/test_gap008_spine_summary_shape.py`](../../tests/ci/test_gap008_spine_summary_shape.py)); **Next:** Slice **21+** — planning only until chartered.
- 2026-04-25: **Tasks 439–448** — **Slice 19** — `MainWindowStatusBarCoordinatorShellBridge`; `Gap008Slice19Tests` + `MainWindowStatusBarCoordinatorShellBridgeTests`; filter superset; spine count see reconciliation + `.buildlogs/gap008_spine/`; **Next:** superseded by Slice **20** row above.
- 2026-04-25: **Tasks 429–438** — **Slice 18** — `MainWindowStatusStripMetricsShellBridge`; `Gap008Slice18Tests` + `MainWindowStatusStripMetricsShellBridgeTests`; `MainWindow.StatusBar.cs` removed; `last_run_summary.json` schema doc + CI fixture; filter superset; spine **139** PASS; **superseded** for “next slice” by **439–448** row above.
- 2026-04-25: **Tasks 418–428** — **Slice 17** — `MainWindowStatusStripClockShellBridge`; `Gap008Slice17Tests` + `MainWindowStatusStripClockShellBridgeTests`; spine reconciliation doc + script artifacts under `.buildlogs/gap008_spine/`; filter superset; **Next:** superseded by Slice **18** row above.
- 2026-04-26: **Tasks 409–417** — **Slice 16** — `MainWindowNotificationCenterShellBridge`; `Gap008Slice16Tests` + `MainWindowNotificationCenterShellBridgeTests`; filter header + superset; spine **118** PASS; `run_verification.py` PASS; Slice 14/15 verification — spine **N** authority (Task 415); **Next:** superseded by Slice **17** row above.
- 2026-04-26: **Tasks 399–408** — **Slice 15** — `MainWindowJumpListDispatchShellBridge`; `Gap008Slice15Tests` + `MainWindowJumpListDispatchShellBridgeTests`; Slice 14 brief — **Host runtime proof** debt table (Task 403); `Gap008Slice14Tests` jump-list hook pin updated for Slice 15 wiring; canonical filter superset; spine **114** PASS; `run_verification.py` PASS; **Next:** superseded by Slice **16** row above.
- 2026-04-25: **Tasks 388–398** — **Slice 14** — `MainWindowFileActivationShellBridge`; `Gap008Slice14Tests` + `MainWindowFileActivationShellBridgeTests`; Slice 13 brief + `Gap008Slice13Tests` — Closed-only **`OnClosedPrelude`** vs **`Cleanup`/`RunCleanupCore`** / finalizer asymmetry pin; canonical filter superset + [`scripts/Run-Gap008MainWindowRegressionTests.ps1`](../scripts/Run-Gap008MainWindowRegressionTests.ps1); spine **106** PASS; `run_verification.py` PASS; **Next:** superseded by Slice **15** row above.
- 2026-04-25: **Tasks 378–387** — **Slice 13** — `MainWindowLifetimeCleanupShellBridge` + channel records; `Gap008Slice13Tests` + `MainWindowLifetimeCleanupShellBridgeTests`; canonical regression filter [`tools/gap008_mainwindow_regression_filter.txt`](../tools/gap008_mainwindow_regression_filter.txt) + [`scripts/Run-Gap008MainWindowRegressionTests.ps1`](../scripts/Run-Gap008MainWindowRegressionTests.ps1); widened spine **97** tests PASS; [`docs/developer/VERIFICATION_PROOF_ARTIFACTS.md`](../developer/VERIFICATION_PROOF_ARTIFACTS.md); **Next:** superseded by Slice **14** row above.
- 2026-04-25: **Tasks 369–377** — [startup truth visual proof](../reports/verification/PROOF_GAP008_STARTUP_TRUTH_VISUAL_2026-04-25.md); startup-truth brief **closed** (§A–§D); **Slice 12** — `MainWindowJumpListTaskbarProgressShellBridge`; `Gap008Slice12Tests` + `MainWindowJumpListTaskbarProgressShellBridgeTests`; widened GAP-008 filter (**88** tests); `run_verification.py` PASS; **Next:** Slice **13+** — chartered brief only.
- 2026-04-25: **GAP-008 Slice 11** — `MainWindowStartupWelcomeActivationShellBridge`; `MainWindow` `Activated` forward; `Gap008Slice11Tests` + `MainWindowStartupWelcomeActivationShellBridgeTests`; startup-truth retention pins +1 in `Gap008StartupTruthTests`; widened regression filter (**84** tests); **Next:** Slice 12 — **landed** same wave (see row above).
- 2026-04-25: **GAP-008 Startup truth recovery** — `StatusBarCoordinator` + `IStartupStateService.StateChanged` + `ApplyPrimaryStatusText`; `Gap008StartupTruthTests` (pins + Task 364 retention); prerequisite for Slice 11; superseded for “next” by Slice 11 row above.
- 2026-04-25: **GAP-008 Slice 10** — `MainWindowToolCatalogShellBridge` + launcher + diagnostics; `MainWindow` wiring + `WireToolCatalogHandlers`; `MainWindowToolCatalogShellBridgeTests` + `Gap008Slice10Tests`; widened regression filter (**71** tests); anti-sprawl subsection (Task 348); **Next:** Slice 11 — welcome/startup (**planning only**).
- 2026-04-25: **GAP-008 Slice 9** — `MainWindowToolbarCommandShellBridge` + `IToolbarShellImportFromToolbar`; `ToolbarViewModel` / `CustomizableToolbar` / `AppServices` / `MainWindow` wiring; `MainWindowToolbarCommandShellBridgeTests` + `Gap008Slice9Tests`; palette diagnostics seam (`ICommandPaletteShellDiagnostics`, Task 333); **Next:** Slice 10 — tool catalog shell (landed same day; see row above).
- 2026-04-25: **GAP-008 Slice 8** — `MainWindowCommandPaletteShellBridge` + `ICommandPaletteShellLauncher`; `MainWindowCommandPaletteShellBridgeTests` + `Gap008Slice8Tests`; Ctrl+P / `ShowCommandPalette` explicit errors; Slice 6 UI-host testing debt documented; narrow-seam forbidden one-liners extended. **Next:** Slice 9 — toolbar command routing (landed same wave; see row above).
- 2026-04-25: **GAP-008 Slice 7** — `MainWindowToolbarCustomizationShellBridge` + `IToolbarCustomizationDialogLauncher`; `MainWindowToolbarCustomizationShellBridgeTests` + `Gap008Slice7Tests`; customization launcher only (palette/catalog deferred to Slice 8+). *(Historical “Next: Slice 8” — Slice 8 landed same wave.)*
- 2026-04-25: **GAP-008 Slice 5** — `MainWindowRecentProjectsMutationBridge` + `IRecentProjectsMutationCommands`; `MainWindowRecentProjectsMutationBridgeTests` + `Gap008Slice5Tests`; Option A (menu population on `MainWindow`). **Next:** Slice 6 — search overlay / toolbar (this plan § only; no code in Slice 5 batch).
- 2026-04-24: **GAP-008 Slice 6** — `MainWindowSearchOverlayShellBridge`; `MainWindowSearchOverlayShellBridgeTests` + `Gap008Slice6Tests`; search overlay shell only (toolbar → Slice 7 planning). **Next:** Slice 7 — toolbar / command palette glue (planning only until chartered).
- 2026-04-25: **GAP-008 Slice 4** — `MainWindowProjectWorkflowBridge`; `Gap008Slice4Tests` + `MainWindowProjectWorkflowBridgeTests` (9 total filtered); Choice A (recents flyout deferred). **Narrow-seam guardrails** recorded under **Already extracted**. **Superseded** by Slice 5 row above for “next slice” pointer.
- 2026-04-25: **GAP-008 Slice 3** — `MainWindowLoadedTailBootstrap` + `MainWindowLoadedTailHooks`; Loaded ordering preserved (bootstrap → DEBUG → tail); `Gap008Slice3Tests` (5). **NavButtonActionSink guardrail:** exists only to bridge **`ShellNavigationCoordinator`** ctor ordering vs **`MainWindowNavigationShellBridge.SetActiveNavButton`** — not a general event bus; new responsibilities require a new bounded slice + ADR (see Slice 2 brief §Guardrail). **Superseded same-day** by Slice 4 row above for “next slice” pointer.
- 2026-04-25: **GAP-008 Slice 2** — `MainWindowNavigationShellBridge` + `NavButtonActionSink`; `NavigationService.NavigationChanged` lifecycle + nav rail `SetActiveNavButton` + command/nav forwards; `Gap008Slice2Tests` (6). **Superseded same-day** by Slice 3 tail extraction row above.
- 2026-04-24: **GAP-008 Slice 1** — Loaded-time bootstrap block (ErrorDialogService root through title bar init) extracted to `MainWindowShellLoadedBootstrap`; seam tests `Gap008Slice1Tests`. Prior “Next Slice: Navigation-Shell” line referred to coordinator work already partially done; **navigation-shell polish** is the next chartered slice **after** this bootstrap cut (see bounded brief).
- 2026-03-16: Transport Wave 4 complete. Marked Status Bar, Transport Shortcut, Import Workflow as done. Next slice = Navigation-Shell Behavior.
- 2026-03-15: Initial plan; next slice = Status Bar Orchestration (Transport Coherence Wave 2 Task 9).
