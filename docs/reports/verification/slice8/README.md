# Slice 8 verification artifacts

Place operator-captured evidence here:

- `ui_synthesis_success.png` (or `.jpg`) — Voice Synthesis panel after successful run.
- `slice8_output.wav` — bytes from `GET /api/audio/file/{audio_id}` after a real `xtts_v2` synthesis.
- `backend_log_snippet.txt` (optional) — one or more lines showing non-stub synthesis (no `synthesis_stub`).

If this folder is empty, Slice 8 UI proof is **not** complete for closure.
