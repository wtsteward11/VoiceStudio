<UserControl x:Class="VoiceStudio.App.Controls.WaveformControl"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
  xmlns:local="using:VoiceStudio.App.Controls"
  x:DataType="local:WaveformControl">
  <Grid x:Name="WaveformContainer" Background="Transparent">
    <canvas:CanvasControl x:Name="WaveCanvas"
      ClearColor="Transparent"
      HorizontalAlignment="Stretch"
      VerticalAlignment="Stretch"
      Visibility="Collapsed" />
    <Path x:Name="WaveformPath"
      Stroke="Cyan"
      StrokeThickness="1.5"
      StrokeLineJoin="Round"
      StrokeEndLineCap="Round"
      StrokeStartLineCap="Round"
      Fill="Cyan"
      HorizontalAlignment="Stretch"
      VerticalAlignment="Stretch"
      Stretch="None" />
    <Line x:Name="PlaybackLine"
      Stroke="Yellow"
      StrokeThickness="2"
      Visibility="Collapsed"
      StrokeDashArray="5,5" />
    <!-- Empty state -->
    <TextBlock Text="No waveform data available"
      HorizontalAlignment="Center"
      VerticalAlignment="Center"
      Foreground="{StaticResource VSQ.Text.SecondaryBrush}"
      Opacity="0.5"
      Visibility="Collapsed"
      x:Name="EmptyStateText" />
  </Grid>
</UserControl>
